# =============================================================================
# demo 环境敏感配置模板 —— 复制为 backend.env 并填写真实值（backend.env 已被 gitignore）
# =============================================================================
# 与 root .env.example 取值一致；生产环境禁止使用这些默认密码
EASYORANGE_DB_PASSWORD=easyorange_app_dev
REDIS_PASSWORD=easyorange123
RABBITMQ_PASSWORD=easyorange123
# AI 供应商密钥（可选，留空则对应 AI 功能降级/不可用，不影响应用启动）
DEEPSEEK_API_KEY=
QWEN_VL_API_KEY=
EMBEDDING_API_KEY=
