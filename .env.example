# ===================================================================
# EasyOrange 环境变量配置文件
# ===================================================================
# 使用说明：
# 1. 复制此文件为 .env（根目录下）
# 2. IDEA 运行、Maven 构建、Docker Compose 均从此文件读取
# 3. .env 文件已被 .gitignore 排除，不会提交到版本控制
# ===================================================================
# 提示：所有变量在 application.yaml 中都有合理的默认值，
#       仅需在需要覆盖默认值时设置。至少无需修改即可本地启动。
# ===================================================================

# -------------------------------------------------------------------
# MySQL 数据库配置
# - Docker Compose 使用 MYSQL_ROOT_PASSWORD 初始化数据库容器
# - Spring Boot 使用 EASYORANGE_* 系列变量连接数据库
# -------------------------------------------------------------------
MYSQL_ROOT_PASSWORD=root123456
EASYORANGE_DB_HOST=localhost
EASYORANGE_DB_PORT=3306
EASYORANGE_DB_NAME=easyorange
EASYORANGE_DB_USERNAME=easyorange_app
EASYORANGE_DB_PASSWORD=easyorange_app_dev

# -------------------------------------------------------------------
# Redis 配置
# -------------------------------------------------------------------
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=easyorange123

# -------------------------------------------------------------------
# RabbitMQ 配置（默认值：localhost:5672, user=easyorange, pass=easyorange123）
# -------------------------------------------------------------------
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=easyorange
RABBITMQ_PASSWORD=easyorange123

# -------------------------------------------------------------------
# JWT 密钥（生产环境必须设置强密钥，长度至少 32 字符）
# -------------------------------------------------------------------
JWT_SECRET_KEY=dev-key-change-in-prod-easyorange-at-least-32-chars

# -------------------------------------------------------------------
# 跨域配置（开发环境默认 http://localhost:5173）
# -------------------------------------------------------------------
CORS_ALLOWED_ORIGINS=http://localhost:5173

# -------------------------------------------------------------------
# Spring Profile（Docker Compose 使用 prod，本地 IDEA 默认 dev）
# -------------------------------------------------------------------
SPRING_PROFILES_ACTIVE=dev

# -------------------------------------------------------------------
# AI API 密钥（可选，不影响应用启动，功能级依赖）
# -------------------------------------------------------------------
# DEEPSEEK_API_KEY=your-deepseek-api-key
# QWEN_VL_API_KEY=your-qwen-vl-api-key
