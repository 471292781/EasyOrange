# EasyOrange — 项目规范

> **唯一规范来源是 [AGENTS.md](./AGENTS.md)**：技术栈、数据库表、状态机、错误码、模块依赖、开发规范（含后端/前端约定）、常用命令。编码细则见 `.claude/rules/ecc/`（ECC 分层规则集，按文件路径自动激活）。

Claude Code 原生支持读取 `AGENTS.md`，本文件仅为向后兼容的跳转页，不再维护独立内容。修改规范请直接改 `AGENTS.md`。
