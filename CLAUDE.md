# EasyOrange — 项目规范

> **唯一规范来源是 [AGENTS.md](./AGENTS.md)**：定位 / 项目结构 / 技术栈 / 全局硬约束 / 参考索引（数据库表、状态机、错误码、开发规范、常用命令等已拆至 `doc/agents/`，见其「参考索引」）。编码细则见 `.claude/rules/ecc/`（ECC 分层规则集，按文件路径自动激活）。

Claude 不会自动常驻加载 `AGENTS.md`，仅在需要时按需读取。本文件仅为向后兼容的跳转页，不再维护独立内容；规范修改一律直接改 `AGENTS.md`。
