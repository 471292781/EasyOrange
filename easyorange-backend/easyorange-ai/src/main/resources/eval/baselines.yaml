# ===================================================================
# EasyOrange 金标准质量基线（Baselines）
# 用途：评估门禁（EvalGate）的对比基准 — 回归分数低于「基线 - 容忍度(0.3)」即卡 build
# 口径：golden-set.yaml 对应 scope 用例的 Judge 平均分（1-5）
# 维护：跑 GoldenSetRegressionIT 得到新基线后，经评审更新本文件（分数只允许合理波动，不追高分）
# ===================================================================
chat: 4.0
