# ArchUnit 配置
# FreezingArchRule 存储必须提交进仓库（ArchUnit 标准做法）：store 本就存在于
# src/test/resources/archunit_store/，CI 的 clean 构建会用它对比「相对上次快照的新增违规」。
# 快照随违规变化而更新（无新违规时写入但内容不变），仅当故意接受新债务/重构移除时提交其变更。
freeze.store.default.allowStoreCreation=true
freeze.store.default.path=src/test/resources/archunit_store