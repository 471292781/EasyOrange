# EasyOrange DDD 架构规范

本文档定义了 EasyOrange 项目的 DDD（领域驱动设计）+ 六边形架构规范。
CQRS 作为可选模式，在读写模型差异明显时引入。

> **约定**：标注 `[现状]` 的内容表示当前代码已实现；标注 `[演进]` 的内容表示架构目标，需逐步落地。

## 一、模块总览

| 模块名 | DDD 架构角色 | 核心定位 | 能放什么 |
|-----------|------------|---------|---------|
| easyorange-backend | 父工程（Parent POM） | 全局依赖 / 版本管理 | 统一依赖版本号、插件管理、模块聚合 |
| easyorange-common | 通用基础模块 | 所有模块可依赖 | 统一响应体、基础异常、通用工具类、领域事件基类、基础枚举 |
| easyorange-framework | 框架适配层 | 基础设施通用封装 | Spring/MyBatis/Redis/MQ **通用配置**、过滤器、AOP 切面、JWT 工具、全局异常处理器 |
| easyorange-message | 消息业务模块 | 站内信/通知限界上下文 | 消息实体、消息模板、离线消息、WebSocket、订阅管理 |
| easyorange-application | 启动聚合层 | 主入口 | 启动类、全局配置 |
| easyorange-user | 用户限界上下文 | 完整 DDD 业务模块 | adapter/application/domain 全分层 |
| easyorange-product | 商品限界上下文 | 完整 DDD 业务模块 | adapter/application/domain 全分层 |
| easyorange-order | 订单限界上下文 | 完整 DDD 业务模块 | adapter/application/domain 全分层 |
| easyorange-payment | 支付限界上下文 | 完整 DDD 业务模块 | adapter/application/domain 全分层 |
| easyorange-favorite | 收藏限界上下文 | 完整 DDD 业务模块 | adapter/application/domain 全分层 |

### 模块演进规划 `[演进]`

当前 `easyorange-common` 承担了技术工具和领域共享两重职责。随着业务增长，可拆分为：

| 演进后模块名 | 定位 | 内容 |
|-------------|------|------|
| easyorange-common-tech | 纯技术通用底座 | 纯 JDK 工具类、统一响应体、基础异常、纯技术常量（无框架/中间件依赖） |
| easyorange-common-domain | 领域共享内核 | 共享值对象、领域事件基类、聚合根基类、全局业务枚举 |

拆分判断标准：当 `common` 模块中领域相关代码（值对象、领域事件基类、业务枚举）占比超过 30%，或业务模块需要仅依赖领域部分而排斥技术工具时，启动拆分。

## 二、核心公共模块完整结构

### 1. easyorange-common（通用基础模块）

包含通用工具、统一响应、基础异常、领域事件基类等所有模块共享的基础定义：

```
easyorange-common
├── com.cartethyia.easyorange.common
│   ├── annotation            # 通用注解 `[现状]`
│   │   ├── Log.java          # 操作日志注解
│   │   ├── RateLimiter.java  # 限流注解
│   │   └── RepeatSubmit.java # 防重复提交注解
│   ├── constant              # 通用常量
│   │   └── CommonConstant.java
│   ├── dto                   # 通用 DTO
│   │   ├── AuthUser.java     # 认证用户信息
│   │   └── PageRequest.java  # 分页请求
│   ├── enums                 # 通用枚举
│   │   ├── IResultCode.java  # 结果码接口
│   │   ├── ResultCode.java   # 通用结果码
│   │   ├── BusinessType.java # 业务操作类型
│   │   ├── LimitType.java    # 限流类型
│   │   └── FileResultCode.java # 文件处理结果码
│   ├── event                 # 领域事件基础设施
│   │   ├── BaseDomainEvent.java    # 领域事件基类
│   │   ├── DomainEventPublisher.java # 事件发布者接口
│   │   └── DomainEventSubscriber.java # 事件订阅者接口
│   ├── exception             # 基础异常
│   │   ├── BaseBusinessException.java  # 业务异常基类（含 code + httpStatus）
│   │   ├── BusinessException.java      # 通用业务异常
│   │   ├── ParamValidationException.java # 参数校验异常
│   │   ├── FileException.java          # 文件异常
│   │   ├── FileSizeLimitExceededException.java
│   │   └── InvalidExtensionException.java
│   ├── notification          # 通知接口
│   │   └── NotificationService.java
│   ├── result                # 统一响应体
│   │   ├── Result.java       # 统一响应体 R<T>
│   │   └── PageResult.java   # 分页结果（纯 Java record，无框架依赖）
│   └── util                  # 通用工具类
│       ├── BizRequire.java   # 业务断言工具
│       ├── FileSizeFormat.java # 文件大小格式化工具
│       ├── MaskUtils.java    # 脱敏工具
│       └── SnowflakeIdGenerator.java # 雪花ID生成器
```

> **现状**：`BaseDO`、`RequestUtil`、`FileUtils` 已迁移到 framework 模块。common 模块现在仅包含纯 JDK 工具和基础定义，无框架依赖。

#### common 与 framework 的边界

| 维度 | common | framework |
|------|--------|-----------|
| 依赖范围 | JDK + Jackson + Spring Core（仅 HttpStatus 枚举） | 依赖 Spring 完整生态/Redis/MQ/MyBatis-Plus 等框架库 |
| 内容类型 | 基础定义、工具类、异常定义、枚举 | 配置类、过滤器、AOP 切面、安全拦截、服务实现 |
| 典型类 | `Result`、`BizRequire`、`BaseDomainEvent`、`FileSizeFormat` | `RedisConfig`、`JwtAuthenticationFilter`、`BaseDO`、`RequestUtil` |
| 是否启动 Spring 上下文 | 否（纯定义和工具） | 是（含 `@Configuration`、`@Service` 等） |

**迁移规则**：如果一个类正常运行需要 Spring 容器初始化（如注入 Bean、使用 `@Configuration`），则必须放在 `easyorange-framework`。

> **现状**：`BaseDO`、`RequestUtil`、`FileUtils` 已从 common 迁移到 framework 模块。common 模块现在仅依赖 JDK + Jackson + Spring Core（用于 HttpStatus 枚举），实现了框架依赖的最小化。

### 2. easyorange-framework（框架适配层）

放和 Spring / 中间件相关的通用封装，和 common 区分开：

```
easyorange-framework
├── com.cartethyia.easyorange.framework
│   ├── aspectj               # AOP 切面
│   │   ├── OperLogAspect.java      # 操作日志切面
│   │   ├── RateLimiterAspect.java  # 限流切面
│   │   └── RepeatSubmitAspect.java # 防重复提交切面
│   ├── config                # 框架配置
│   │   ├── async/            # 异步配置
│   │   │   ├── JacksonConfig.java
│   │   │   ├── LoggingRejectedExecutionHandler.java
│   │   │   └── ThreadPoolConfig.java
│   │   ├── cache/            # 本地缓存配置
│   │   │   └── LocalCacheConfig.java
│   │   ├── database/         # 数据库配置
│   │   │   └── MybatisPlusConfig.java
│   │   ├── http/             # HTTP 客户端配置
│   │   │   └── RestClientConfig.java
│   │   ├── properties/       # 配置属性类
│   │   │   ├── JwtProperties.java
│   │   │   ├── OperLogProperties.java
│   │   │   ├── RateLimiterProperties.java
│   │   │   ├── SecurityProperties.java
│   │   │   ├── ThreadPoolProperties.java
│   │   │   └── WebMvcProperties.java
│   │   ├── redis/            # Redis 配置
│   │   │   ├── CacheConfig.java
│   │   │   └── RedisConfig.java
│   │   ├── security/         # 安全配置
│   │   │   ├── JsonLogoutSuccessHandler.java
│   │   │   └── SecurityConfig.java
│   │   ├── web/              # WebMVC 配置（拦截器、统一响应）
│   │   │   ├── WebMvcConfig.java
│   │   │   ├── ResponseAdvice.java
│   │   │   └── RequestConfig.java
│   │   └── OpenApiYamlController.java
│   ├── constant              # 框架层常量
│   │   └── LoginCacheConstants.java
│   ├── entity                # 框架层数据对象
│   │   └── BaseDO.java       # `[现状]` 已从 common 迁移到此处
│   ├── event                 # 领域事件基础设施实现
│   │   ├── DomainEventPublisherImpl.java
│   │   ├── DomainEventPersistenceService.java
│   │   └── idempotency/
│   │       └── EventIdempotencyChecker.java
│   ├── exception             # 全局异常处理器
│   │   ├── GlobalExceptionHandler.java
│   │   └── CacheTypeMismatchException.java
│   ├── file                  # 文件服务 `[现状]`
│   │   ├── controller/
│   │   │   └── FileController.java
│   │   ├── dto/
│   │   │   └── UploadFileVO.java
│   │   ├── entity/
│   │   │   └── UploadFile.java
│   │   ├── mapper/
│   │   │   └── UploadFileMapper.java
│   │   └── service/
│   │       ├── FileService.java
│   │       ├── ImageProcessingService.java
│   │       └── impl/
│   │           ├── FileServiceImpl.java
│   │           └── ImageProcessingServiceImpl.java
│   ├── filter                # Web 过滤器
│   │   ├── JwtAuthenticationFilter.java
│   │   ├── XssFilter.java
│   │   └── XssHttpServletRequestWrapper.java
│   ├── handler               # 处理器
│   │   ├── CustomMetaObjectHandler.java # MyBatis-Plus 自动填充
│   │   ├── JsonAuthenticationEntryPoint.java
│   │   └── LoggingInterceptor.java
│   ├── manager               # 异步管理器
│   │   └── AsyncManager.java
│   ├── notification          # 通知服务实现
│   │   └── DefaultNotificationServiceImpl.java
│   ├── operlog               # 操作日志 `[现状]`
│   │   ├── dto/
│   │   │   └── LogStorageStats.java
│   │   ├── entity/
│   │   │   └── SysOperLog.java
│   │   ├── mapper/
│   │   │   └── SysOperLogMapper.java
│   │   └── service/
│   │       ├── SysOperLogService.java
│   │       ├── OperLogArchiveService.java
│   │       └── impl/
│   │           └── SysOperLogServiceImpl.java
│   ├── redis                 # Redis 缓存抽象
│   │   ├── RedisCache.java
│   │   └── impl/
│   │       └── RedisCacheImpl.java
│   ├── service               # 框架服务
│   │   ├── TokenService.java
│   │   └── impl/
│   │       └── TokenServiceImpl.java
│   └── util                  # 框架工具
│       ├── FileUtils.java          # 文件上传工具（依赖 Spring MultipartFile）
│       ├── JwtUtil.java            # JWT 工具
│       ├── OperLogUtil.java        # 操作日志工具
│       ├── RequestUtil.java        # 请求工具（依赖 Servlet API）
│       └── SecurityContextUtil.java # 安全上下文工具
```

#### GlobalExceptionHandler 的定位 `[现状]`

当前 `GlobalExceptionHandler` 放在 `easyorange-framework` 中，原因是：
- 它处理的是所有模块通用的异常映射逻辑（BusinessException → HTTP 响应）
- 它依赖框架层的异常类型（Spring Security、Validation 等）
- 通过 Spring Boot 自动配置注册，所有模块自动生效

`[演进]` 如果未来需要处理模块特有的异常映射（如特定业务错误码的特殊处理），可在 `easyorange-application` 中添加补充的异常处理器，优先级高于 framework 中的通用处理器。

### 3. easyorange-message（消息业务模块）`[现状]`

当前 `easyorange-message` 是一个完整的业务限界上下文，负责站内信/通知功能，已采用完整 DDD 分层：

```
easyorange-message
├── com.cartethyia.easyorange.message
│   ├── application             # 应用层（CQRS 模式）
│   │   ├── command/            # 命令处理
│   │   │   ├── DeleteMessageCommand.java
│   │   │   ├── MarkAsReadCommand.java
│   │   │   ├── MarkAsReadBatchCommand.java
│   │   │   ├── MessageCommandHandler.java
│   │   │   ├── SendMessageCommand.java
│   │   │   └── SendSystemMessageCommand.java
│   │   └── query/              # 查询处理
│   │       ├── MessageQuery.java
│   │       ├── MessageQueryHandler.java
│   │       └── UnreadCountQuery.java
│   ├── constant                # 消息常量
│   │   └── MessageConstant.java
│   ├── controller              # 消息接口
│   │   ├── MessageCommandController.java
│   │   └── MessageQueryController.java
│   ├── domain                  # 领域层
│   │   ├── event/              # 领域事件
│   │   │   ├── MessageDeletedEvent.java
│   │   │   ├── MessageReadEvent.java
│   │   │   └── MessageSentEvent.java
│   │   ├── exception/          # 领域异常
│   │   │   ├── MessageDomainException.java
│   │   │   ├── MessageNotFoundException.java
│   │   │   └── UnauthorizedOperationException.java
│   │   ├── repository/         # 仓储接口
│   │   │   ├── MessageRepository.java
│   │   │   ├── MessageSubscriptionRepository.java
│   │   │   ├── MessageTemplateRepository.java
│   │   │   ├── OfflineMessageRepository.java
│   │   │   └── query/          # 查询仓储
│   │   │       ├── MessageQueryRepository.java
│   │   │       └── MybatisMessageQueryRepository.java
│   │   ├── service/            # 领域服务
│   │   │   ├── MessageRoutingService.java
│   │   │   └── OfflineMessageStoreService.java
│   │   └── valueobject/        # 值对象
│   │       ├── MessageContent.java
│   │       ├── MessageType.java
│   │       └── Recipient.java
│   ├── dto                     # 数据传输对象
│   │   ├── request/
│   │   └── vo/
│   ├── entity                  # 消息实体（持久化层）
│   │   ├── Message.java
│   │   ├── MessageSubscription.java
│   │   ├── MessageTemplate.java
│   │   └── OfflineMessage.java
│   ├── enums                   # 消息枚举
│   │   ├── MessageResultCode.java
│   │   ├── MessageStatus.java
│   │   ├── MessageType.java
│   │   └── ReadStatus.java
│   ├── mapper                  # MyBatis Mapper
│   │   ├── MessageMapper.java
│   │   ├── MessageSubscriptionMapper.java
│   │   ├── MessageTemplateMapper.java
│   │   └── OfflineMessageMapper.java
│   ├── service                 # 应用服务（传统模式）
│   │   ├── MessageSubscriptionService.java
│   │   ├── MessageTemplateService.java
│   │   ├── OfflineMessageService.java
│   │   └── impl/
│   └── websocket               # WebSocket 支持
│       ├── WebSocketAuthInterceptor.java
│       ├── WebSocketConfig.java
│       ├── WebSocketEventListener.java
│       ├── WebSocketMessageHandler.java
│       └── WebSocketNotifier.java
```

#### 消息模块演进规划 `[演进]`

当前消息模块同时承担了「业务消息（站内信）」和「跨模块事件通信基础设施」两重职责。随着业务增长，应拆分为：

| 演进后模块 | 定位 | 内容 |
|-----------|------|------|
| easyorange-message-infra | 跨模块事件通信基础设施 | Outbox 框架、MQ 路由/重试/死信、事件分发 |
| easyorange-notification | 通知业务限界上下文 | 站内信、消息模板、离线消息、WebSocket、订阅管理 |

**Outbox 模式** `[演进]`：当引入 `easyorange-message-infra` 时，实现事务性事件发布：

```
业务事务开始
  ├─ 写入业务数据（如 user 表）
  └─ 写入 Outbox 表（同一事务）
业务事务提交

OutboxPublisher 定时任务（独立线程）
  ├─ 扫描 status = PENDING 的 Outbox 记录
  ├─ 发送到 MQ
  ├─ 更新 status = PUBLISHED
  └─ 失败则增加 retry_count，超过阈值标记 DEAD_LETTER
```

**重要约束**：`easyorange-message-infra` 模块不得定义任何业务领域事件类。具体事件（如 `UserRegisteredEvent`、`OrderCreatedEvent`）必须在各自业务模块的 `domain/event/` 下定义。业务事件只需实现 `common` 中的 `BaseDomainEvent` 基类即可被 Outbox 框架统一处理。

### 4. easyorange-application（启动聚合层）

```
easyorange-application
├── com.cartethyia.easyorange
│   ├── EasyOrangeApplication.java  # 主启动类
│   └── controller                  # 全局接口
│       ├── HealthController.java   # 健康检查
│       └── PlatformStatsController.java # 平台统计
```

> **说明** `[现状]`：当前 `HealthController` 和 `PlatformStatsController` 放在 application 模块，因为它们不属于任何特定业务模块。如果后续平台统计功能复杂化，应抽取为独立模块。

## 三、单个业务模块完整结构

以 `easyorange-user` 为例，其他业务模块直接照搬此结构。遵循「六边形架构 + DDD」：

```
easyorange-user
├── com.cartethyia.easyorange.user
│   ├── adapter                 # 六边形适配器层（内外交互）
│   │   ├── inbound             # 输入适配器（外部→模块）
│   │   │   └── web             # REST 接口
│   │   │       ├── controller  # 控制器
│   │   │       │   ├── AuthController.java
│   │   │       │   └── UserController.java
│   │   │       ├── dto         # 接口入参/出参
│   │   │       │   ├── request
│   │   │       │   │   ├── LoginRequest.java
│   │   │       │   │   ├── RegisterRequest.java
│   │   │       │   │   ├── ChangePasswordRequest.java
│   │   │       │   │   └── UpdateUserRequest.java
│   │   │       │   └── response
│   │   │       │       ├── LoginResponse.java
│   │   │       │       ├── UserVO.java
│   │   │       │       └── UserProfileVO.java
│   │   │       └── validation  # 入参校验
│   │   │           ├── Password.java
│   │   │           ├── PasswordValidator.java
│   │   │           ├── Unique.java
│   │   │           └── UniqueFieldValidator.java
│   │   └── outbound            # 输出适配器（模块→外部，实现输出端口）
│   │       ├── persistence     # 数据库适配（实现仓储端口）
│   │       │   ├── UserEntity.java    # 数据库实体（DO）
│   │       │   ├── UserMapper.java    # MyBatis Mapper
│   │       │   └── UserRepositoryImpl.java  # 仓储实现
│   │       ├── cache           # 缓存适配
│   │       │   ├── RedisLoginAttemptAdapter.java
│   │       │   └── RedisSmsCodeAdapter.java
│   │       ├── messaging       # 消息发送适配
│   │       │   └── UserEventPublisher.java
│   │       ├── storage         # 文件存储适配
│   │       │   └── LocalAvatarFileStorage.java
│   │       └── util            # 基础设施工具
│   │           └── NicknameGenerator.java
│   ├── application             # 应用层（用例编排，事务管理）
│   │   ├── assembler           # 对象转换器
│   │   │   └── UserAssembler.java
│   │   └── service             # 应用服务（按用例拆分，单一职责）
│   │       ├── UserRegistrationAppService.java   # 注册用例
│   │       ├── UserLoginAppService.java          # 登录/登出/刷新
│   │       ├── PasswordResetAppService.java      # 忘记密码
│   │       ├── SmsCodeAppService.java            # 发送验证码
│   │       └── UserAppService.java               # 用户信息管理
│   ├── domain                  # 领域层（最纯净，无框架依赖）
│   │   ├── aggregate           # 聚合根（领域核心）
│   │   │   └── User.java
│   │   ├── constant            # 领域常量
│   │   │   ├── UserConstant.java
│   │   │   └── UserSecurityConstant.java
│   │   ├── valueobject         # 模块私有值对象（必须不可变）
│   │   │   ├── UserProfile.java
│   │   │   ├── LoginInfo.java
│   │   │   └── AuditInfo.java
│   │   ├── event               # 模块私有领域事件
│   │   │   ├── UserRegisteredEvent.java
│   │   │   ├── PasswordChangedEvent.java
│   │   │   └── ForgotPasswordEvent.java
│   │   ├── service             # 领域服务（纯业务逻辑，无状态，无框架注解）
│   │   │   ├── AuthenticationDomainService.java
│   │   │   ├── LoginSecurityDomainService.java
│   │   │   ├── PasswordDomainService.java
│   │   │   ├── SmsCodeDomainService.java
│   │   │   └── UserRegistrationDomainService.java
│   │   ├── enums               # 领域业务枚举
│   │   │   ├── UserStatus.java
│   │   │   ├── UserType.java
│   │   │   ├── Sex.java
│   │   │   ├── LoginMethod.java
│   │   │   ├── ClientType.java
│   │   │   ├── DelFlag.java
│   │   │   └── UserResultCode.java
│   │   ├── exception           # 模块领域内部异常
│   │   │   └── UserDomainException.java
│   │   ├── port                # 六边形端口（抽象接口）
│   │   │   └── output/                  # 出站端口（领域层 → 外部基础设施）
│   │   │       ├── OutboundPort.java        # 出站端口标记接口
│   │   │       ├── AvatarFilePort.java      # 头像文件端口
│   │   │       ├── LoginAttemptPort.java    # 登录尝试端口
│   │   │       ├── PasswordEncoderPort.java # 密码编码端口
│   │   │       ├── SmsCodePort.java         # 短信验证码端口
│   │   │       ├── SmsRateLimitPort.java    # 短信频率限制端口
│   │   │       └── UserEventPort.java       # 用户事件端口
│   │   └── repository          # 仓储端口（聚合根存取接口）
│   │       └── UserRepository.java
│   └── config                  # 模块专属配置（归入基础设施关注点）
│       └── UserDomainConfig.java  # 领域服务 Bean 配置
```

**包结构调整说明：**

- 去掉了 `infrastructure` 层，所有端口实现统一归入 `adapter/outbound`（纯粹的六边形架构）
- 去掉 `domain/port/input`（输入端口），当只有一个实现类时属于过度抽象（YAGNI 原则）
- `config` 包属于基础设施关注点，保留在模块根包下
- `application/service` 替代 `application/command|query`，采用传统应用服务模式；读写差异大时再拆分为 CQRS
- `domain/port/output/` 下放置所有出站端口接口，`OutboundPort` 作为标记接口统一标识
- 包结构采用 `domain/port/output/` 而非扁平的 `domain/port/`，以明确表达端口方向（出站 vs 入站），与 order、payment 等模块保持一致
- `domain/repository/` 作为特殊的出站端口，专门定义聚合根的存取接口
- 领域常量放在 `domain/constant/`，技术常量（路由前缀、缓存 Key、MQ Topic）应下沉到对应适配器层
- 领域服务不使用 `@Service` 等框架注解，通过 `config/` 下的配置类手动注册 Bean
- 领域服务依赖外部功能时，通过端口接口抽象，由适配器层实现
- **应用服务按用例拆分**：避免单个 `AuthAppService` 处理所有认证相关功能，应按具体用例拆分为 `UserRegistrationAppService`、`UserLoginAppService`、`PasswordResetAppService` 等，每个类只负责一个完整的用户场景

> **现状**：user 模块已完成 DDD 架构优化，领域层完全纯净（无框架依赖），`infrastructure` 包已迁移到 `adapter/outbound/util/`。
> - `PasswordEncoderPort` 由 `easyorange-framework` 的 `SecurityConfig` 提供的 `BCryptPasswordEncoder` 自动适配，无需在 user 模块中创建独立适配器类。
>
> **缓存统一规范**：所有业务模块的缓存适配器必须遵循以下规则：
> 1. **端口定义在 `domain/port/output/`**：如 `OrderCachePort`、`ProductCachePort`
> 2. **实现放在 `adapter/outbound/cache/`**：如 `RedisOrderCacheAdapter`、`ProductCacheAdapter`
> 3. **禁止在 `infrastructure/` 包下放置缓存实现**：`infrastructure` 包仅保留模块级配置（如 `UserDomainConfig`）
> 4. **技术常量下沉到适配器层**：Redis Key 前缀、过期时间等放在 `adapter/outbound/cache/` 下的常量类中
> 5. **应用层只依赖端口**：`OrderCommandHandler`、`OrderQueryHandler` 等通过 `OrderCachePort` 接口操作缓存，不直接依赖 `RedisTemplate`

### 其他业务模块结构参考

**easyorange-product** `[现状]`：

product 模块已采用完整 DDD 分层 + CQRS 模式：

```
easyorange-product
├── adapter/inbound/web/
│   ├── controller/           # 控制器
│   │   ├── ProductController.java
│   │   ├── ProductQueryController.java
│   │   ├── ProductReportController.java
│   │   └── SearchController.java
│   └── dto/
│       ├── request/          # 请求 DTO
│       │   ├── ProductCreateRequest.java
│       │   ├── ProductQueryRequest.java
│       │   ├── ProductSearchRequest.java
│       │   ├── ProductUpdateRequest.java
│       │   └── ReportRequest.java
│       └── response/         # 响应 DTO
│           ├── CategoryResponse.java
│           ├── HotKeywordResponse.java
│           ├── ProductReportResponse.java
│           ├── ProductResponse.java
│           ├── SearchHistoryResponse.java
│           └── SearchResultResponse.java
├── adapter/outbound/
│   ├── cache/                # 缓存适配器（实现缓存端口）
│   │   ├── ProductCacheAdapter.java
│   │   ├── CategoryCacheAdapter.java
│   │   └── ProductCacheConstant.java  # 缓存技术常量
│   └── persistence/
│       ├── converter/            # DO 转换器
│       │   └── ProductConverter.java
│       ├── dataobject/           # 数据对象
│       │   ├── CategoryDO.java
│       │   ├── CategoryProductCountDO.java
│       │   ├── HotKeywordDO.java
│       │   ├── ProductDO.java
│       │   ├── ProductDetailDO.java
│       │   ├── ProductImageDO.java
│       │   ├── ProductReportDO.java
│       │   └── SearchHistoryDO.java
│       ├── mapper/               # MyBatis Mapper
│       │   ├── CategoryMapper.java
│       │   ├── HotKeywordMapper.java
│       │   ├── ProductDetailMapper.java
│       │   ├── ProductImageMapper.java
│       │   ├── ProductMapper.java
│       │   ├── ProductReportMapper.java
│       │   └── SearchHistoryMapper.java
│       ├── repository/           # 仓储实现
│       │   ├── CategoryQueryRepositoryImpl.java
│       │   ├── ProductQueryRepositoryImpl.java
│       │   ├── ProductReportRepositoryImpl.java
│       │   └── ProductRepositoryImpl.java
│       └── ProductSnapshotPortImpl.java
├── application/              # 应用层（CQRS 模式）
│   ├── command/              # 命令侧
│   │   ├── dto/
│   │   │   ├── CreateProductCommand.java
│   │   │   ├── DecrementStockCommand.java
│   │   │   ├── DeleteProductCommand.java
│   │   │   ├── MarkAsSoldCommand.java
│   │   │   ├── RestoreStockCommand.java
│   │   │   └── UpdateProductCommand.java
│   │   ├── handler/
│   │   │   ├── CreateProductReportHandler.java
│   │   │   └── ProcessProductReportHandler.java
│   │   └── ProductCommandService.java
│   ├── event/                # 事件监听
│   │   └── ProductEventListener.java
│   ├── query/                # 查询侧
│   │   ├── assembler/
│   │   │   └── ProductReadModelAssembler.java
│   │   ├── dto/
│   │   │   ├── ProductDetailQuery.java
│   │   │   ├── ProductQuery.java
│   │   │   ├── ProductVO.java
│   │   │   └── SellerInfo.java
│   │   ├── handler/
│   │   │   ├── GetPendingReportsHandler.java
│   │   │   └── ProductSearchHandler.java
│   │   ├── readmodel/        # 读模型
│   │   │   ├── CategoryReadModel.java
│   │   │   ├── HotKeywordReadModel.java
│   │   │   ├── ProductReadModel.java
│   │   │   ├── SearchHistoryReadModel.java
│   │   │   └── SellerReadModel.java
│   │   └── ProductQueryService.java
│   └── service/              # 应用服务
│       ├── ProductViewCountService.java
│       └── SearchHistoryService.java
├── config/                   # 模块配置
│   └── ProductDomainConfig.java  # 领域服务 Bean 配置
└── domain/                   # 领域层
    ├── aggregate/            # 聚合根
    │   └── Product.java
    ├── constant/             # 领域常量（业务相关）
    │   └── ProductConstant.java
    ├── entity/               # 领域实体
    │   ├── HotKeyword.java
    │   ├── ProductDetail.java
    │   ├── ProductReport.java
    │   └── SearchHistory.java
    ├── enums/                # 领域枚举
    │   ├── ConditionLevel.java
    │   ├── ProductReportStatus.java
    │   ├── ProductResultCode.java
    │   └── ProductStatus.java
    ├── event/                # 领域事件
    │   ├── ProductCreatedEvent.java
    │   ├── ProductDeletedEvent.java
    │   ├── ProductMarkedSoldEvent.java
    │   ├── ProductUpdatedEvent.java
    │   ├── StockDecreasedEvent.java
    │   └── StockRestoredEvent.java
    ├── exception/            # 领域异常
    │   ├── InsufficientStockException.java
    │   ├── InvalidProductStatusException.java
    │   └── ProductNotFoundException.java
    ├── port/                 # 端口
    │   ├── CategoryCachePort.java
    │   ├── ProductCachePort.java
    │   └── ProductSnapshotPort.java
    ├── repository/           # 仓储接口
    │   ├── query/
    │   │   ├── CategoryQueryRepository.java
    │   │   └── ProductQueryRepository.java
    │   ├── ProductReportRepository.java
    │   └── ProductRepository.java
    ├── service/              # 领域服务（纯 POJO，无框架注解）
    │   └── ProductReportDomainService.java
    └── valueobject/          # 值对象
        ├── CategoryId.java
        ├── ContactMethod.java
        ├── ImageSet.java
        ├── ImageUrl.java
        ├── Money.java
        ├── ProductDescription.java
        ├── ProductId.java
        ├── ProductTitle.java
        ├── SellerId.java
        ├── StockQuantity.java
        ├── TagSet.java
        ├── TradeLocation.java
        └── Version.java
```

> **优化说明**：
> - 缓存服务（ProductCacheService、CategoryCacheService）已迁移到 `adapter/outbound/cache/`，改名为 Adapter
> - 领域服务 `ProductReportDomainService` 已移除 `@Service`、`@Transactional` 注解，通过 `ProductDomainConfig` 注册 Bean
> - 技术常量（Redis Key、Cron 表达式）已分离到 `adapter/outbound/cache/ProductCacheConstant.java`
> - 领域层 `ProductConstant` 仅保留业务常量（热度阈值）

**easyorange-favorite** `[现状]`：

当前 favorite 模块采用混合架构，部分遵循 DDD 分层：

```
easyorange-favorite
├── controller/                # 控制器（根包）
│   ├── request/               # 请求 DTO
│   │   └── BatchRemoveRequest.java
│   └── FavoriteController.java
├── domain/                    # 领域层
│   ├── aggregate/             # 聚合根
│   │   └── Favorite.java
│   └── repository/            # 仓储接口
│       └── FavoriteRepository.java
├── infrastructure/            # 基础设施层
│   ├── acl/                   # 防腐层（调用 product 模块）
│   │   ├── ProductAclService.java
│   │   └── ProductAclServiceImpl.java
│   └── persistence/           # 持久化
│       ├── FavoriteDO.java
│       ├── FavoriteMapper.java
│       └── MybatisFavoriteRepository.java
└── service/                   # 应用服务
    ├── dto/                   # DTO
    │   ├── AddFavoriteDTO.java
    │   ├── FavoritePageQuery.java
    │   ├── FavoriteVO.java
    │   └── RemoveFavoriteDTO.java
    └── FavoriteService.java
```

`[演进]` 目标是将 favorite 模块演进为完整 DDD 分层：
- 将 `controller/` 移至 `adapter/inbound/web/`
- 将 `service/` 移至 `application/service/`
- 将 `infrastructure/` 拆分为 `adapter/outbound/`

**easyorange-order** `[现状]`：

order 模块已采用完整 DDD 分层 + Saga 模式：

```
easyorange-order
├── adapter/inbound/
│   ├── job/                   # 定时任务
│   │   ├── OrderAutoConfirmTask.java
│   │   └── OrderTimeoutTask.java
│   ├── mq/subscriber/         # 消息订阅
│   │   └── Order*EventSubscriber.java
│   └── web/
│       ├── controller/
│       │   ├── OrderCommandController.java
│       │   └── OrderQueryController.java
│       └── dto/
├── adapter/outbound/
│   ├── cache/                 # 缓存适配器（实现缓存端口）
│   │   └── RedisOrderCacheAdapter.java
│   ├── messaging/             # 消息适配器
│   │   ├── PaymentGatewayAdapter.java
│   │   ├── ProductInventoryAdapter.java
│   │   ├── ProductQueryAdapter.java
│   │   └── UserInfoAdapter.java
│   └── persistence/
│       ├── MybatisOrderRepository.java
│       ├── OrderDO.java
│       ├── OrderDataConverter.java
│       ├── OrderMapper.java
│       ├── SagaDO.java
│       ├── SagaMapper.java
│       └── SagaRepositoryImpl.java
├── application/
│   ├── assembler/
│   ├── command/               # 命令处理
│   │   ├── CancelOrderCommand.java
│   │   ├── ConfirmReceiptCommand.java
│   │   ├── CreateOrderCommand.java
│   │   ├── OrderCommandHandler.java
│   │   ├── PayOrderCommand.java
│   │   ├── RefundOrderCommand.java
│   │   └── ShipOrderCommand.java
│   └── query/                 # 查询处理
├── domain/
│   ├── aggregate/
│   │   └── OrderAggregate.java
│   ├── constant/              # 领域常量（业务相关）
│   │   ├── OrderConstant.java         # AUTO_CONFIRM_DAYS 等业务常量
│   │   ├── OrderResultCode.java       # 错误码枚举
│   │   └── OrderStatus.java           # 订单状态枚举
│   ├── event/
│   ├── exception/
│   ├── port/output/           # 输出端口
│   │   ├── OrderCachePort.java        # 缓存端口
│   │   ├── OrderReadRepository.java
│   │   ├── OrderRepository.java
│   │   ├── PaymentGatewayPort.java
│   │   ├── ProductInventoryPort.java
│   │   ├── ProductQueryPort.java
│   │   └── UserInfoPort.java
│   ├── saga/                  # Saga 编排
│   │   ├── CreateOrderSaga.java
│   │   ├── SagaRepository.java
│   │   ├── SagaState.java
│   │   └── SagaStatus.java
│   └── valueobject/
└── infrastructure/            # 基础设施层（模块级配置和技术常量）
    ├── cache/
    │   └── OrderCacheConstant.java    # Redis Key 前缀、过期时间等技术常量
    └── config/
        └── OrderTimeoutProperties.java # 订单超时配置（@ConfigurationProperties）
```

> **优化说明**：
> - 移除了顶层 `constant/` 目录，遵循 DDD 分层原则
> - 领域常量（`AUTO_CONFIRM_DAYS`）保留在 `domain/constant/OrderConstant.java`
> - 技术常量（Redis Key、过期时间）分离到 `infrastructure/cache/OrderCacheConstant.java`
> - 配置属性类（`OrderTimeoutProperties`）移至 `infrastructure/config/`
> - `infrastructure/` 包仅保留模块级配置和技术常量，端口实现仍在 `adapter/outbound/`

**easyorange-payment** `[现状]`：

payment 模块已采用完整 DDD 分层 + Outbox 模式：

```
easyorange-payment
├── adapter/inbound/web/
│   ├── assembler/
│   ├── request/
│   ├── response/
│   ├── MockPaymentController.java
│   ├── PaymentCommandController.java
│   └── PaymentQueryController.java
├── adapter/outbound/
│   ├── gateway/
│   │   └── PaymentGatewayAdapter.java
│   └── persistence/
│       ├── converter/
│       ├── mapper/
│       ├── po/
│       ├── JdbcDomainEventStore.java
│       ├── MybatisIdempotencyKeyRepository.java
│       ├── MybatisPaymentRepository.java
│       └── PaymentConfigRepository.java
├── application/
│   ├── command/               # 命令处理
│   ├── event/                 # 事件处理（含 OutboxEventPublisher）
│   ├── idempotency/           # 幂等性服务
│   ├── lock/                  # 分布式锁
│   ├── metrics/               # 指标服务
│   ├── mock/                  # Mock 支付
│   └── query/                 # 查询处理
├── constant/
│   └── PaymentConstant.java
├── domain/
│   ├── aggregate/
│   │   └── PaymentAggregate.java
│   ├── constant/
│   │   ├── PaymentMethod.java
│   │   ├── PaymentResultCode.java
│   │   └── PaymentStatus.java
│   ├── event/
│   ├── exception/
│   ├── factory/
│   ├── port/output/           # 输出端口
│   ├── saga/
│   ├── specification/
│   └── valueobject/
└── infrastructure/
    └── security/
        └── CallbackSignatureVerifier.java
```

## 四、模块依赖铁律（必须遵守，防止耦合）

1. **easyorange-common**：所有模块可依赖，提供基础定义和工具
2. **easyorange-framework**：依赖 easyorange-common，提供框架封装
3. **easyorange-message**：依赖 easyorange-common + easyorange-framework
4. **业务模块（user/product/order/payment/favorite）**：只能依赖 easyorange-common + easyorange-framework
5. **easyorange-application**：依赖所有业务模块（仅聚合启动用）
6. **业务模块之间禁止直接依赖**：跨模块通信通过事件驱动或 RPC 调用实现
7. **强一致性跨模块场景** `[演进]`：允许通过 `adapter/outbound/rpc` 的 Feign 客户端调用，但必须在 ACL 中转换外部模型

```
依赖方向（箭头表示 "被依赖"）：

common ← framework ← message
   ↑         ↑
   └── 业务模块 (user/product/order/payment/favorite)
              ↑
       application (聚合启动)
```

## 五、DDD 领域设计规范

### 1. 聚合根设计

**聚合根职责：**

- 保证聚合内的业务不变量（Invariants）
- 作为聚合的唯一访问入口
- 通过工厂方法创建，确保创建时满足不变量

**当前实践** `[现状]`：

项目使用 Lombok `@Builder(toBuilder = true)` 实现不可变聚合根，通过 `toBuilder()` 实现返回新实例的"修改"模式：

```java
@Getter
@Builder(toBuilder = true)
public class User {
    private final Long id;
    private final String username;
    private final String password;
    private final UserProfile profile;
    private final LoginInfo loginInfo;
    private final AuditInfo auditInfo;

    public static User register(String username, String encodedPassword, String nickName) {
        Objects.requireNonNull(username, "username must not be null");
        Objects.requireNonNull(encodedPassword, "password must not be null");
        return User.builder()
            .username(username)
            .password(encodedPassword)
            .status(UserStatus.NORMAL)
            .profile(new UserProfile(null, null, null, nickName, null, null, null))
            .loginInfo(LoginInfo.initial())
            .build();
    }

    public User changePassword(String encodedNewPassword, Long operatorId) {
        Objects.requireNonNull(encodedNewPassword, "password must not be null");
        return this.toBuilder()
            .password(encodedNewPassword)
            .loginInfo(this.loginInfo.updatePasswordTime())
            .auditInfo(updateAuditInfo(operatorId))
            .build();
    }
}
```

**聚合边界规则：**

- 一个事务只能修改一个聚合根
- 聚合根通过 ID 引用其他聚合，不直接持有其他聚合的引用
- 聚合内的所有修改必须通过聚合根的方法进行
- 聚合根字段必须为 `final`，修改操作返回新实例

### 2. 值对象设计

值对象强制要求：

- **不可变**：使用 `final` 字段或 `record`
- **基于属性的相等性**：实现 `equals()` 和 `hashCode()`（record 自动实现）
- **自验证**：创建时校验业务规则
- **替换而非修改**：通过返回新实例实现"修改"

```java
public record UserProfile(
    String email,
    String phone,
    Sex sex,
    String nickName,
    String realName,
    String avatar,
    String studentId
) {
    public UserProfile updateEmail(String email) {
        return new UserProfile(email, this.phone, this.sex, this.nickName,
            this.realName, this.avatar, this.studentId);
    }

    public UserProfile updatePhone(String phone) {
        return new UserProfile(this.email, phone, this.sex, this.nickName,
            this.realName, this.avatar, this.studentId);
    }

    public UserProfile updateAvatar(String avatar) {
        return new UserProfile(this.email, this.phone, this.sex, this.nickName,
            this.realName, avatar, this.studentId);
    }
}
```

### 3. 领域服务

- **无状态**：不持有任何可变状态
- **纯业务逻辑**：不包含框架调用、数据库操作
- **多聚合协调**：当业务逻辑涉及多个聚合时使用领域服务
- **与应用层区分**：领域服务处理纯业务规则，应用层处理用例编排和事务管理

```java
public class AuthenticationDomainService {
    public void validateRegistration(User user, List<User> existingUsers) {
        if (existingUsers.stream().anyMatch(u -> u.getProfile().phone().equals(user.getProfile().phone()))) {
            throw new UserDomainException("手机号已注册");
        }
    }
}
```

### 4. 仓储设计

- **接口在 domain/repository**：定义以聚合根为操作对象的接口
- **实现在 adapter/outbound/persistence**：内部处理 DO 与聚合根的转换
- **返回聚合根，不返回 DO**：仓储的使用者只关心领域模型

```java
// domain/repository/UserRepository.java
public interface UserRepository {
    Optional<User> findById(Long id);
    User save(User user);
    void deleteById(Long id);
    Optional<User> findByUsername(String username);
}

// adapter/outbound/persistence/UserRepositoryImpl.java
public class UserRepositoryImpl implements UserRepository {
    private final UserMapper mapper;

    @Override
    public User save(User user) {
        UserEntity entity = UserAssembler.toEntity(user);
        mapper.insertOrUpdate(entity);
        return UserAssembler.toDomain(entity);
    }
}
```

### 5. 领域事件

- **事件基类**：所有领域事件继承 `common` 中的 `BaseDomainEvent`
- **事件命名**：使用过去时态（UserRegistered、OrderPaid、PasswordChanged）
- **事件内容**：仅包含必要 ID 和状态，不传输完整聚合
- **事件发布** `[现状]`：通过 `@PublishEvent` 注解 + AOP 切面实现，事务提交后异步发布

```java
// 领域事件定义
public class UserRegisteredEvent extends BaseDomainEvent {
    private final Long userId;
    private final String username;
}

// 应用服务中发布事件
@PublishEvent(type = "UserRegistered", extractor = "userRegisteredEventExtractor")
@Transactional(rollbackFor = Exception.class)
public Long register(RegisterRequest request) {
    // 业务逻辑
}
```

`[演进]` 当引入 Outbox 模式后，事件发布改为同一事务写入 Outbox 表，由 OutboxPublisher 异步发布到 MQ，保证事务一致性。

### 6. 错误码体系与异常传播

#### 错误码分层

| 错误码定义位置 | 前缀/模式 | 示例 |
|-----------|------|------|
| `common/enums/ResultCode` | 通用系统错误码 | `INTERNAL_SERVER_ERROR`、`VALIDATE_FAILED`、`FORBIDDEN`、`NOT_FOUND` |
| 各模块 `domain/enums/*ResultCode` | 模块业务错误码 | `UserResultCode`、`ProductResultCode`、`MessageResultCode` |

#### 异常继承体系

```
RuntimeException
├── BaseBusinessException (common, 业务异常基类)
│   ├── code: String          # 错误码
│   ├── message: String       # 错误消息
│   └── httpStatus: HttpStatus # HTTP 状态码
│   ├── BusinessException (common, 通用业务异常)
│   ├── ParamValidationException (common, 参数校验异常)
│   ├── FileException (common, 文件异常)
│   ├── UserDomainException (user 模块, 领域层专用)
│   ├── ProductNotFoundException (product 模块)
│   └── ... 其他模块异常
```

**划分原则：**
- `BaseBusinessException` — 所有业务异常的基类，提供统一的 code + message + httpStatus
- `BusinessException` — 通用业务异常，用于不需要自定义子类的场景
- 各模块异常 — 继承 `BaseBusinessException`，通过错误码区分具体业务场景
- 领域层异常（如 `UserDomainException`）— 领域层专用，不含任何框架依赖

#### 异常传播路径

```
领域层 (throw UserDomainException)
  → 应用层 (无需 catch，事务自动回滚)
    → 适配层 (GlobalExceptionHandler 拦截)
      → HTTP 响应 (JSON: { "code": "VALIDATE_FAILED", "message": "用户不存在" })
```

**关键规则：**

1. **领域层抛异常**：当业务不变量被违反时，直接抛出模块级领域异常，不 try-catch
2. **应用层不吞异常**：应用服务通常不处理领域异常，让事务回滚；仅在需要自定义错误映射时 catch
3. **全局统一拦截**：`GlobalExceptionHandler` 在 `easyorange-framework` 中统一拦截，确保 API 响应格式一致
4. **RPC 异常映射** `[演进]`：跨模块 Feign 调用时，调用方需 catch FeignException 并转换为自己的业务异常，防止外部异常类型泄漏

```java
// 领域层异常（不含任何框架引用，纯 JDK）
public class UserDomainException extends RuntimeException {
    public UserDomainException(String message) {
        super(message);
    }
}

// 应用层/适配层使用通用业务异常
throw BusinessException.of(UserResultCode.USER_NOT_FOUND);
```

> **注意**：避免使用 `static final` 异常实例（如 `public static final UserBizException USER_NOT_FOUND = ...`），因为所有抛出点共享同一堆栈跟踪，导致调试困难。应使用 `throw new UserBizException(...)` 每次创建新实例。

## 六、防腐层（ACL）设计 `[演进]`

当跨模块通过 RPC 调用或集成外部系统时，必须使用防腐层将外部模型转换为内部模型：

```java
// adapter/outbound/rpc/acl/OrderAcl.java
public class OrderAcl {
    private final OrderFeignClient client;

    // 外部 OrderDTO → 内部 OrderSummary 值对象
    public OrderSummary toOrderSummary(OrderDTO externalOrder) {
        return new OrderSummary(
            externalOrder.getId(),
            externalOrder.getAmount(),
            OrderStatus.from(externalOrder.getStatus())
        );
    }

    public List<OrderSummary> getRecentOrders(Long userId) {
        return client.getOrdersByUserId(userId)
            .stream()
            .map(this::toOrderSummary)
            .toList();
    }
}
```

### 跨模块 RPC 版本兼容策略

当业务模块之间通过 Feign 直接调用时（强一致性场景），必须考虑 RPC 接口的版本兼容：

**接口变更兼容规则：**

| 变更类型 | 兼容性 | 处理方式 |
|---------|--------|---------|
| 新增字段（响应体） | 向前兼容 | 调用方 ACL 忽略未知字段，使用 `@JsonIgnoreProperties(ignoreUnknown = true)` |
| 新增可选参数（请求体） | 向前兼容 | 提供方设置默认值，调用方可选传递 |
| 删除/重命名字段 | **不兼容** | 提供方必须保留旧字段并用 `@Deprecated` 标记，至少保留一个迭代周期 |
| 修改字段类型 | **不兼容** | 必须新增接口版本，旧接口保留一个迭代周期 |

**ACL 作为兼容性边界：**

ACL 层承担兼容性适配职责——当提供方接口升级时，只需要修改调用方模块的 ACL 代码，调用方上层业务逻辑不受影响。

## 七、测试策略

### 1. 测试分层

| 测试类型 | 测试对象 | 工具 | 覆盖率要求 |
|---------|---------|------|----------|
| 单元测试 | 领域层（聚合根、值对象、领域服务） | JUnit 5 + AssertJ | 90%+ |
| 单元测试 | 应用层（应用服务，Mock 端口） | JUnit 5 + Mockito | 80%+ |
| 集成测试 | 仓储实现（真实数据库） | Testcontainers + JUnit 5 | 80%+ |
| 集成测试 | Controller（MockMvc） | Spring Boot Test + MockMvc | 关键路径 100% |
| 架构测试 | DDD 分层规则、包依赖关系 | ArchUnit | 核心规则 100% |

### 2. 领域层单元测试示例

```java
@DisplayName("User 聚合根")
class UserTest {

    @Test
    @DisplayName("register: 正常创建用户")
    void register_validInput_success() {
        var user = User.register("testuser", "encodedPassword", "TestNick");

        assertThat(user.getUsername()).isEqualTo("testuser");
        assertThat(user.getStatus()).isEqualTo(UserStatus.NORMAL);
    }

    @Test
    @DisplayName("register: 用户名为空时抛出异常")
    void register_nullUsername_throws() {
        assertThatThrownBy(() -> User.register(null, "encodedPassword", "TestNick"))
            .isInstanceOf(NullPointerException.class)
            .hasMessageContaining("username");
    }

    @Test
    @DisplayName("changePassword: 返回新实例，原实例不变")
    void changePassword_returnsNewInstance() {
        var user = User.register("testuser", "oldPassword", "TestNick");
        var updated = user.changePassword("newPassword", 1L);

        assertThat(updated.getPassword()).isEqualTo("newPassword");
        assertThat(user.getPassword()).isEqualTo("oldPassword");
    }
}
```

### 3. 应用层单元测试示例

```java
@ExtendWith(MockitoExtension.class)
@DisplayName("AuthAppService")
class AuthAppServiceTest {

    @Mock
    private UserRepository userRepository;

    private AuthAppService service;

    @BeforeEach
    void setUp() {
        service = new AuthAppService(userRepository);
    }

    @Test
    @DisplayName("changePassword: 用户不存在时抛出异常")
    void changePassword_userNotFound_throws() {
        when(userRepository.findById(any())).thenReturn(Optional.empty());

        assertThatThrownBy(() -> service.changePassword(1L, "old", "new"))
            .isInstanceOf(BusinessException.class);
    }
}
```

### 4. 架构测试示例

```java
@AnalyzeClasses(packages = "com.cartethyia.easyorange")
class ArchitectureRulesTest {

    @ArchTest
    static final ArchRule domain_layer_should_not_depend_on_framework =
        noClasses()
            .that().resideInAPackage("..domain..")
            .should().dependOnClassesThat()
            .resideInAnyPackage(
                "org.springframework..",
                "com.baomidou..",
                "org.apache.redis.."
            );

    @ArchTest
    static final ArchRule business_modules_should_not_depend_on_each_other =
        slices().matching("com.cartethyia.easyorange.(*)..")
            .should().notDependOnEachOther();
}
```

## 八、落地步骤（循序渐进）

### 第一步：完善 easyorange-common `[现状]`

已完成：
- 通用工具、统一响应体、基础异常、领域事件基类整理完毕
- 清理不必要的框架依赖（BaseDO、RequestUtil、FileUtils 已迁移到 framework）
- PageResult 改为纯 Java record，无框架依赖

### 第二步：完善 easyorange-framework `[现状]`

已完成：
- MyBatis、Redis、Security 的通用配置和基类写好
- 业务模块可直接继承使用，无需重复写配置
- 删除了废弃的 CacheUtils

### 第三步：完善 easyorange-user 模块 `[现状]`

已完成 DDD 架构优化：
- 领域层完全纯净，无框架依赖
- 领域服务不使用 `@Service` 注解，通过配置类注册 Bean
- `infrastructure` 包迁移到 `adapter/outbound/util/`
- 领域常量移至 `domain/constant/`
- 新增 `PasswordEncoderPort` 端口抽象密码编码功能，由 `easyorange-framework` 的 `BCryptPasswordEncoder` Bean 自动适配

### 第四步：完善 easyorange-product 模块 `[现状]`

已完成 DDD 架构优化：
- 领域服务 `ProductReportDomainService` 移除框架注解，通过 `ProductDomainConfig` 注册 Bean
- 缓存服务迁移到 `adapter/outbound/cache/`，改名为 Adapter
- 技术常量（Redis Key、Cron 表达式）分离到 `adapter/outbound/cache/ProductCacheConstant.java`
- 领域层 `ProductConstant` 仅保留业务常量（热度阈值）

### 第五步：其他业务模块照搬 user/product 模块的结构 `[演进]`

每个模块只需要替换业务相关的内容（比如 order 模块的聚合根、应用服务），包结构完全复用，统一规范。

### 第六步：补全 favorite 模块的 DDD 分层 `[演进]`

将 favorite 模块从扁平结构演进为完整的 adapter/application/domain 分层。

### 第七步 `[演进]`：引入 Outbox 模式

当跨模块事件通信需要强一致性保证时，实现 Outbox 模式。

### 第八步 `[演进]`：common 模块拆分

当业务增长到需要区分技术工具和领域共享时，将 `easyorange-common` 拆分为 `easyorange-common-tech` 和 `easyorange-common-domain`。

## 九、关键细节避坑

### 1. 领域层绝对纯净

`domain` 包下的所有代码，**不能 import 任何 Spring、MyBatis、Redis 的 API**，只能依赖 JDK 和 `easyorange-common` 中的纯定义类（枚举、异常基类、事件基类等）。

### 2. 值对象不可变性

所有值对象必须不可变：
- 使用 `record` 或 `final` 字段
- 不暴露可变引用（返回防御性拷贝）
- 通过返回新实例实现"修改"

### 3. 聚合根不可变性

聚合根字段必须为 `final`，修改操作返回新实例。当前使用 Lombok `@Builder(toBuilder = true)` 模式实现。

### 4. 领域服务持久化职责

**原则：领域服务应自己完成聚合的持久化，而不是将聚合返回给应用服务再保存。**

```java
// BAD — 领域服务只创建对象，应用服务负责保存，逻辑分散
public class UserRegistrationDomainService {
    public User register(...) {
        User user = User.register(...);
        return user; // 不保存
    }
}

// Application 层
public Long register(RegisterRequest request) {
    User user = userRegistrationDomainService.register(...);
    User saved = userRepository.save(user); // 持久化逻辑在应用层
    return saved.getId();
}

// GOOD — 领域服务自己完成持久化，逻辑内聚
public class UserRegistrationDomainService {
    private final UserRepository userRepository;
    
    public User register(...) {
        User user = User.register(...);
        return userRepository.save(user); // 领域服务自己持久化
    }
}

// Application 层只负责编排
public Long register(RegisterRequest request) {
    User saved = userRegistrationDomainService.register(...);
    userEventPort.publish(new UserRegisteredEvent(saved.getId(), saved.getUsername()));
    return saved.getId();
}
```

**例外情况**：当应用服务需要在领域服务执行后、持久化前插入额外逻辑（如事件发布需获取 ID），可将持久化留在应用层。但应尽量让领域服务完成完整的业务闭环。

### 5. 聚合根、领域服务、应用服务职责速查

| 层级 | 一句话职责 | 判断标准 |
|------|-----------|---------|
| **聚合根** | **"我变我自己"** — 只负责自己的状态变更 | 只修改自己的字段，不查别人 |
| **Domain Service** | **"我帮你们协调"** — 跨聚合/跨实体的业务逻辑 | 需要查其他聚合、调用外部端口、协调多个对象 |
| **Application Service** | **"我负责跑腿"** — 用例编排、事务、DTO转换 | 调用 Domain Service → 转 DTO → 返回 |

**典型归属判断：**

| 场景 | 归属 | 原因 |
|------|------|------|
| `user.changePassword()` | 聚合根 | 只涉及 User 自己的状态变更 |
| `user.recordLogin()` | 聚合根 | 只涉及 User 自己的 LoginInfo 更新 |
| 检查用户名是否已存在 | Domain Service | 需要查询**其他** User 聚合 |
| 密码加密比对 | Domain Service | 涉及**外部能力**（PasswordEncoderPort）|
| 登录安全策略（失败次数锁定）| Domain Service | 涉及**另一个概念**（LoginAttempt）|
| 短信验证码校验 | Domain Service | 涉及**另一个聚合**（SmsCode）|
| 登录成功后组装 Token 返回 | Application Service | 用例编排 + DTO 组装 |

### 6. 常量分层放置

| 常量类型 | 放置位置 | 示例 |
|---------|---------|------|
| 业务枚举 / 状态 | `domain/enums` | UserStatus、ProductStatus |
| 业务常量（非枚举） | `domain/constant` | UserConstant、ProductConstant |
| 全局共享业务枚举 | `common/enums` | ResultCode、BusinessType |
| 全局技术常量 | `common/constant` | CommonConstant |
| 框架层常量 | `framework/constant` | LoginCacheConstants |
| 模块业务错误码 | `domain/enums/*ResultCode` | UserResultCode、ProductResultCode |

**包命名规范：**

- **枚举包用复数 `enums/`**：`enum` 是 Java 关键字，不能作为包名；使用复数形式避免冲突，符合行业惯例
- **常量包用复数 `constants/`**：与 `enums/` 保持一致，语义清晰
- **枚举和常量分开存放**：枚举是类型定义，常量是静态 final 字段，分开存放便于维护和扩展

> **注意** `[现状]`：当前项目所有模块统一使用 `constant/`（单数），如 `common/constant/`、`user/constant/`、`framework/constant/` 等。`[演进]` 目标是逐步统一为 `constants/`（复数）以与 `enums/` 保持一致。

**原则：常量应紧贴其使用者所在的层，而不是放在模块根级别。**

### 7. 应用服务 vs 领域服务

| 维度 | 应用服务 | 领域服务 |
|-----|---------|---------|
| 职责 | 用例编排、事务管理、端口协调 | 纯业务规则、多聚合协调、聚合持久化 |
| 依赖 | 依赖领域服务 + 输出端口 | 仅依赖领域模型 + 仓储接口 |
| 框架 | 可包含 @Transactional 等注解 | 绝对无框架依赖 |
| 返回值 | DTO/VO | 聚合根/值对象 |
| 拆分原则 | **按用例拆分**，一个类一个完整场景 | **按业务领域拆分**，一个类一个业务子域 |

**应用服务拆分原则：**

- **禁止"上帝类"**：单个应用服务不应处理超过 3 个不相关的用例
- **按完整用户场景拆分**：注册、登录、密码重置、验证码发送应各自独立
- **命名规范**：`{场景}AppService`，如 `UserRegistrationAppService`、`UserLoginAppService`
- **事务边界**：每个应用服务方法就是一个完整的事务边界

**领域服务设计原则：**

- **自己完成持久化**：领域服务应直接调用 `Repository` 完成聚合的保存和更新，而不是将聚合返回给应用服务再持久化
- **禁止方法参数注入依赖**：所有依赖必须通过构造函数注入，禁止将 `DomainService` 作为方法参数传递
- **依赖接口而非实现**：领域服务依赖的 Repository、Port、其他 Domain Service 都必须是接口，不得依赖具体实现类
- **示例对比**：

```java
// BAD — 方法参数注入依赖，破坏封装
public User authenticateBySms(String phone, String code, SmsCodeDomainService smsService) {
    smsService.verifyCode(phone, code);
    // ...
}

// GOOD — 构造函数注入，方法签名纯净
public class AuthenticationDomainService {
    private final SmsCodeDomainService smsCodeDomainService;
    
    public AuthenticationDomainService(SmsCodeDomainService smsCodeDomainService) {
        this.smsCodeDomainService = smsCodeDomainService;
    }
    
    public User authenticateBySms(String phone, String code) {
        smsCodeDomainService.verifyCode(phone, code);
        // ...
    }
}
```

**领域服务依赖数量控制：**

- **理想情况**：1-2 个依赖（Repository + 1 个 Port/DomainService）
- **可接受**：3 个依赖
- **警告**：4 个及以上依赖，应考虑按业务子域拆分

```java
// 可接受：3 个依赖
public class UserRegistrationDomainService {
    private final UserRepository userRepository;
    private final PasswordDomainService passwordDomainService;
}

// 警告：4 个依赖，建议拆分
public class AuthenticationDomainService {
    private final UserRepository userRepository;
    private final PasswordDomainService passwordDomainService;
    private final LoginSecurityDomainService loginSecurityDomainService;
    private final SmsCodeDomainService smsCodeDomainService; // 第4个
}

// 优化后：按认证方式拆分
public class PasswordAuthenticationDomainService {
    private final UserRepository userRepository;
    private final PasswordDomainService passwordDomainService;
    private final LoginSecurityDomainService loginSecurityDomainService;
}

public class SmsAuthenticationDomainService {
    private final UserRepository userRepository;
    private final SmsCodeDomainService smsCodeDomainService;
}
```

### 8. CQRS 渐进式引入

- **早期/简单场景**：使用传统应用服务（`UserAppService`），同时处理读写
- **读写模型差异大时**：拆分为 `command/handler` 和 `query/handler`
- **复杂查询场景**：Query 侧可使用物化视图、读库副本、ES 等
- **不推荐一刀切**：简单 CRUD 场景强制 CQRS 会增加大量样板代码

### 9. DO 命名规范

- 数据库实体统一使用 `DO` 后缀或 `Entity` 后缀：`UserEntity`、`ProductDO`
- DO 仅存在于 `adapter/outbound/persistence` 包中，不得泄漏到领域层
- DO 与聚合根之间的转换在 `UserRepositoryImpl` 或 `Assembler` 中完成

### 10. 禁止 static final 异常实例

```java
// BAD — 所有抛出点共享同一堆栈跟踪，调试困难
public static final BusinessException USER_NOT_FOUND =
    BusinessException.of("USER_NOT_FOUND", "用户不存在");

// GOOD — 每次创建新实例，堆栈跟踪准确
throw BusinessException.of(UserResultCode.USER_NOT_FOUND);
```

## 十、架构设计原则

### 1. 六边形架构（端口与适配器）

- **输出端口**：定义依赖外部的接口（如仓储、事件发布、缓存），位于 `domain/port/output` 和 `domain/repository`
- **入站适配器**：将外部请求（REST、MQ、Job）转换为内部调用，位于 `adapter/inbound`
- **出站适配器**：将内部调用转换为外部实现（数据库、MQ、RPC），位于 `adapter/outbound`
- **输入端口（可选）**：仅当有多个实现或需要解耦时定义，单实现时直接调用应用服务（YAGNI）

### 2. DDD 分层职责

| 层 | 职责 | 依赖规则 |
|---|------|---------|
| adapter | 与外部系统交互，格式转换，实现端口 | 依赖 application、domain |
| application | 用例编排，事务管理，端口协调 | 依赖 domain |
| domain | 核心业务逻辑，领域模型，定义端口 | 仅依赖 common 中的纯定义 |

### 3. 依赖倒置原则（DIP）

- 领域层定义端口接口（`domain/port/output/`、`domain/repository/`）
- 适配器层实现端口接口（`adapter/outbound/`）
- 应用层通过端口接口与外部交互，不直接依赖具体实现
- `OutboundPort` 标记接口统一标识所有出站端口

### 4. 防腐层（ACL）

- 跨模块 RPC 调用必须在 ACL 中转换外部模型为内部模型
- 集成外部系统（支付、短信）也必须通过 ACL
- 防止外部模型变化污染内部领域模型

## 十一、数据库迁移规范（Flyway）

### 1. 核心原则

| 原则 | 说明 |
|------|------|
| **DDL 与 DML 分离** | Schema 变更（DDL）和数据迁移（DML）必须分开成独立的迁移文件 |
| **迁移不可变** | 已部署到生产环境的迁移文件禁止修改，只能新增迁移 |
| **自包含迁移** | 每个迁移文件包含该功能所需的完整 DDL（建表、索引、约束） |
| **功能驱动命名** | 一个功能一个迁移文件，不按"索引层"、"约束层"拆分 |

### 2. 迁移文件结构

```
db/migration/
├── V1__init_schema.sql              # DDL: 初始表结构（含索引、约束）
├── V2__seed_data.sql                # DML: 种子数据
├── V3__add_product_review.sql       # DDL: 新功能表结构（含索引、约束）
├── V4__migrate_product_review_data.sql  # DML: 数据迁移（如需要）
└── V5__add_xxx_feature.sql          # DDL: 下一个功能
```

### 3. 命名规范

| 类型 | 命名模式 | 示例 |
|------|---------|------|
| DDL 迁移 | `V{N}__{feature_name}.sql` | `V3__add_product_review.sql` |
| DML 迁移 | `V{N}__migrate_{feature_name}_data.sql` | `V4__migrate_product_review_data.sql` |
| 种子数据 | `V{N}__seed_data.sql` | `V2__seed_data.sql` |
| 开发测试数据 | `R__insert_dev_test_data.sql` | 放在 `db/dev/` 目录 |

### 4. DDL 编写规范

**索引和约束内联定义：**

```sql
CREATE TABLE `eo_product_review` (
    `id` BIGINT NOT NULL COMMENT '主键 ID',
    `product_id` BIGINT NOT NULL COMMENT '商品 ID',
    `user_id` BIGINT NOT NULL COMMENT '评价用户 ID',
    `rating` TINYINT NOT NULL DEFAULT 5 COMMENT '评分（1-5）',
    `content` TEXT NOT NULL COMMENT '评价内容',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态（0 隐藏 1 显示 2 待审核）',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `del_flag` TINYINT NOT NULL DEFAULT 0 COMMENT '删除标志（0 正常 2 删除）',
    `version` INT NOT NULL DEFAULT 0 COMMENT '乐观锁版本号',
    PRIMARY KEY (`id`),
    -- 索引内联定义
    KEY `idx_eo_product_review_product_id` (`product_id`),
    KEY `idx_eo_product_review_user_id` (`user_id`),
    KEY `idx_eo_product_review_create_time` (`create_time` DESC),
    -- 约束内联定义
    CONSTRAINT `chk_eo_product_review_rating` CHECK (`rating` >= 1 AND `rating` <= 5),
    CONSTRAINT `chk_eo_product_review_status` CHECK (`status` IN (0, 1, 2))
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='商品评价表';
```

### 5. DML 编写规范

**使用 MySQL 8.0.20+ 推荐的别名语法：**

```sql
-- GOOD: 使用 AS new 别名（MySQL 8.0.20+）
INSERT INTO `eo_category` (`id`, `name`, `parent_id`, `level`, `sort_order`, `status`)
VALUES (1, '电子数码', 0, 1, 1, 1)
AS new
ON DUPLICATE KEY UPDATE
    `name` = new.`name`,
    `sort_order` = new.`sort_order`,
    `update_time` = NOW();

-- BAD: 使用已弃用的 VALUES() 函数（MySQL 8.0.20+ 弃用）
INSERT INTO `eo_category` (`id`, `name`, `parent_id`, `level`, `sort_order`, `status`)
VALUES (1, '电子数码', 0, 1, 1, 1)
ON DUPLICATE KEY UPDATE
    `name` = VALUES(`name`);  -- 已弃用
```

### 6. Flyway 配置规范

**application.yaml（生产环境）：**

```yaml
spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false
    baseline-version: 0
    validate-on-migrate: true
    clean-disabled: true        # 生产环境禁止 clean
    table: flyway_schema_history
    placeholder-replacement: false
    out-of-order: false        # 生产环境禁止乱序
    ignore-migration-patterns: '*:ignored'
```

**application-dev.yaml（开发环境）：**

```yaml
spring:
  flyway:
    locations: classpath:db/migration,classpath:db/dev
    out-of-order: true         # 开发环境允许乱序，便于团队协作
```

**pom.xml（Maven 插件）：**

```xml
<plugin>
    <groupId>org.flywaydb</groupId>
    <artifactId>flyway-maven-plugin</artifactId>
    <version>11.14.1</version>
    <configuration>
        <url>jdbc:mysql://localhost:3306/easyorange</url>
        <user>easyorange_app</user>
        <password>easyorange_app_dev</password>
        <locations>
            <location>filesystem:src/main/resources/db/migration</location>
        </locations>
        <placeholderReplacement>false</placeholderReplacement>
        <cleanDisabled>true</cleanDisabled>  <!-- 生产环境必须为 true -->
    </configuration>
</plugin>
```

### 7. 常用命令

| 命令 | 说明 | 使用场景 |
|------|------|---------|
| `mvn flyway:info` | 查看迁移状态 | 检查当前版本、待执行迁移 |
| `mvn flyway:migrate` | 执行迁移 | 应用新迁移 |
| `mvn flyway:validate` | 校验迁移 | CI/CD 中验证 checksum |
| `mvn flyway:repair` | 修复历史 | 修复 checksum 不匹配（谨慎使用） |
| `mvn flyway:clean` | 清空数据库 | 仅开发环境使用 |

### 8. 开发流程

**新增功能时：**

1. **创建 DDL 迁移**：`V{N}__add_{feature}.sql`，包含表结构、索引、约束
2. **创建 DML 迁移**（如需要）：`V{N+1}__migrate_{feature}_data.sql`
3. **本地验证**：`mvn flyway:migrate`
4. **提交代码**：迁移文件纳入版本控制

**开发测试数据：**

- 放在 `db/dev/R__insert_dev_test_data.sql`（Repeatable Migration）
- 开发环境自动加载，生产环境不加载

### 9. 反模式（禁止）

| 反模式 | 问题 | 正确做法 |
|--------|------|---------|
| 修改已部署的迁移 | 导致 checksum 不匹配，生产环境执行失败 | 新增迁移文件 |
| DDL 和 DML 混合 | 难以回滚，事务过长 | 分离成独立文件 |
| 按"索引层"拆分 | 每次新表都要改已存在的文件 | 每个功能自包含 |
| NOT NULL 无默认值 | 锁表，重写所有行 | 先加 nullable，backfill 后再加约束 |
| 生产环境执行 clean | 数据丢失 | `clean-disabled: true` |

### 10. 表结构设计规范

| 规范 | 说明 |
|------|------|
| 主键 | 使用 `BIGINT`，禁止自增，由应用层生成（雪花 ID） |
| 字符集 | 统一使用 `utf8mb4`，排序规则 `utf8mb4_0900_ai_ci` |
| 时间字段 | `create_time`、`update_time` 必须有默认值 |
| 软删除 | `del_flag` TINYINT，0 正常，2 删除 |
| 乐观锁 | `version` INT，默认 0 |
| 审计字段 | `create_by`、`update_by` BIGINT |
| 索引命名 | `idx_{table}_{columns}` 或 `uk_{table}_{columns}`（唯一索引） |
| 约束命名 | `chk_{table}_{column}`（CHECK 约束） |
| 外键 | 禁止使用物理外键，通过应用层维护关联 |
