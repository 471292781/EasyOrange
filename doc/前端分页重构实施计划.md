# 前端分页重构 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 统一前端分页类型、组件、补全缺失分页的页面

**Architecture:** 
- 类型层面：删除 `PageData<T>`，全局统一使用 `PageResult<T>`
- 组件层面：引入 shadcn/ui Pagination 组件于 `@/components/ui/pagination.tsx`，AdminTable 和 C 端页面均使用该组件
- 补缺层面：OrdersPage / MyProductsPage / MessagesPage 增加分页状态和控件

**Tech Stack:** React, TypeScript, shadcn/ui, TanStack Query, Tailwind CSS

---

## File Structure

### Files to Create
- `src/components/ui/pagination.tsx` — shadcn/ui Pagination 组件

### Files to Modify
**类型统一 (PageData → PageResult):**
- `src/admin/types/admin.ts` — 删除 `PageData<T>`
- `src/admin/api/adminApi.ts` — 6 处 `PageData<X>` → `PageResult<X>`
- `src/admin/pages/products/ProductManagePage.test.tsx` — PageData → PageResult
- `src/admin/pages/products/ProductReviewPage.test.tsx` — PageData → PageResult
- `src/admin/pages/orders/OrderManagePage.test.tsx` — PageData → PageResult
- `src/admin/pages/users/UserManagePage.test.tsx` — PageData → PageResult
- `src/admin/pages/reports/ReportManagePage.test.tsx` — PageData → PageResult
- `src/admin/pages/reviews/ReviewManagePage.test.tsx` — PageData → PageResult

**AdminTable 改造:**
- `src/admin/components/AdminTable.tsx` — 内联分页 → shadcn Pagination

**C端页面迁移:**
- `src/pages/notifications/NotificationsPage.tsx` — 手写分页 → shadcn Pagination
- `src/pages/notifications/notifications.css` — 删除分页相关 CSS
- `src/pages/favorites/FavoritesPage.tsx` — 手写分页 → shadcn Pagination
- `src/pages/favorites/favorites.css` — 删除分页相关 CSS

**补缺页面:**
- `src/pages/orders/OrdersPage.tsx` — 添加分页状态 + shadcn Pagination
- `src/pages/products/MyProductsPage.tsx` — 添加分页状态 + shadcn Pagination
- `src/api/messageApi.ts` — 添加分页参数
- `src/pages/messages/MessagesPage.tsx` — 添加分页状态 + shadcn Pagination

**SemanticSearch 补全:**
- `src/api/aiApi.ts` — `SemanticSearchResult` 加 `pages` 字段

---

### Task 1: 创建 shadcn/ui Pagination 组件

**Files:**
- Create: `src/components/ui/pagination.tsx`
- Modify: `src/components/ui/index.ts`

- [ ] **Step 1: 创建 `pagination.tsx`**

```tsx
import { ChevronLeft, ChevronRight, MoreHorizontal } from 'lucide-react';
import * as React from 'react';
import { type ButtonProps, buttonVariants } from '@/components/ui/button';
import { cn } from '@/lib/utils';

const Pagination = ({ className, ...props }: React.ComponentProps<'nav'>) => (
    <nav
        role="navigation"
        aria-label="pagination"
        className={cn('mx-auto flex w-full justify-center', className)}
        {...props}
    />
);
Pagination.displayName = 'Pagination';

const PaginationContent = React.forwardRef<HTMLUListElement, React.ComponentProps<'ul'>>(
    ({ className, ...props }, ref) => (
        <ul ref={ref} className={cn('flex flex-row items-center gap-1', className)} {...props} />
    )
);
PaginationContent.displayName = 'PaginationContent';

const PaginationItem = React.forwardRef<HTMLLIElement, React.ComponentProps<'li'>>(
    ({ className, ...props }, ref) => <li ref={ref} className={cn('', className)} {...props} />
);
PaginationItem.displayName = 'PaginationItem';

type PaginationLinkProps = {
    isActive?: boolean;
} & Pick<ButtonProps, 'size'> & React.ComponentProps<'button'>;

const PaginationLink = ({ className, isActive, size = 'icon', ...props }: PaginationLinkProps) => (
    <button
        className={cn(
            buttonVariants({
                variant: isActive ? 'default' : 'ghost',
                size,
            }),
            className
        )}
        aria-current={isActive ? 'page' : undefined}
        {...props}
    />
);
PaginationLink.displayName = 'PaginationLink';

const PaginationPrevious = ({ className, ...props }: React.ComponentProps<typeof PaginationLink>) => (
    <PaginationLink
        aria-label="上一页"
        size="default"
        className={cn('gap-1 pl-2.5', className)}
        {...props}
    >
        <ChevronLeft className="h-4 w-4" />
        <span>上一页</span>
    </PaginationLink>
);
PaginationPrevious.displayName = 'PaginationPrevious';

const PaginationNext = ({ className, ...props }: React.ComponentProps<typeof PaginationLink>) => (
    <PaginationLink
        aria-label="下一页"
        size="default"
        className={cn('gap-1 pr-2.5', className)}
        {...props}
    >
        <span>下一页</span>
        <ChevronRight className="h-4 w-4" />
    </PaginationLink>
);
PaginationNext.displayName = 'PaginationNext';

const PaginationEllipsis = ({ className, ...props }: React.ComponentProps<'span'>) => (
    <span
        aria-hidden
        className={cn('flex h-9 w-9 items-center justify-center', className)}
        {...props}
    >
        <MoreHorizontal className="h-4 w-4" />
        <span className="sr-only">更多</span>
    </span>
);
PaginationEllipsis.displayName = 'PaginationEllipsis';

export {
    Pagination,
    PaginationContent,
    PaginationEllipsis,
    PaginationItem,
    PaginationLink,
    PaginationNext,
    PaginationPrevious,
};
```

- [ ] **Step 2: 在 `index.ts` 中导出**

在 `src/components/ui/index.ts` 末尾添加：

```ts
export {
    Pagination,
    PaginationContent,
    PaginationEllipsis,
    PaginationItem,
    PaginationLink,
    PaginationNext,
    PaginationPrevious,
} from './pagination';
```

- [ ] **Step 3: 验证 TypeScript 编译**

Run: `cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend && npx tsc --noEmit --pretty 2>&1 | head -30`
Expected: 无编译错误（或仅预期内的错误，不涉及 pagination 组件）

---

### Task 2: 类型统一 — 删除 PageData，统一为 PageResult

**Files:**
- Modify: `src/admin/types/admin.ts`
- Modify: `src/admin/api/adminApi.ts`
- Modify: 6 个 admin test 文件

- [ ] **Step 1: 删除 `PageData<T>` 类型定义**

在 `src/admin/types/admin.ts` 中删除：

```
export interface PageData<T> {
    records: T[];
    total: number;
    current: number;
    size: number;
    pages: number;
}
```

- [ ] **Step 2: 更新 `adminApi.ts` 中的类型引用**

在 `src/admin/api/adminApi.ts` 中：
- 将 `import ... PageData ...` 改为 `import type { ... PageResult ... } from '@/types';`
  - 注意：`@/types` 的 `PageResult` 与 `admin/types/admin.ts` 中的 `PageData` 结构完全一致
- 将所有 `PageData<AdminUser>` / `PageData<AdminProduct>` 等替换为 `PageResult<AdminUser>` / `PageResult<AdminProduct>`
- 更新 import 来源：`import type { PageResult } from '@/types';`

具体修改：

```ts
// 第 26 行：从 admin types 导入中移除 PageData
// import ... PageData ...  →  在 admin types 导入中去掉 PageData

// 在最顶部 import 区域添加
import type { PageResult } from '@/types';

// 第 84 行：return request<PageData<AdminUser>>(
//     →  return request<PageResult<AdminUser>>(
// 第 122 行：return request<PageData<AdminProduct>>(
//     →  return request<PageResult<AdminProduct>>(
// 第 161 行：return request<PageData<AdminOrder>>(
//     →  return request<PageResult<AdminOrder>>(
// 第 231 行：return request<PageData<AdminReport>>(
//     →  return request<PageResult<AdminReport>>(
// 第 254 行：return request<PageData<AdminReview>>(
//     →  return request<PageResult<AdminReview>>(
```

- [ ] **Step 3: 更新 6 个 test 文件**

每个 test 文件做同样的事情：
1. 将 `import type { ... PageData } from '../../types/admin'` 改为 `import type { PageResult } from '@/types'`
2. 将所有 `PageData<X>` 替换为 `PageResult<X>`

涉及文件：
- `src/admin/pages/products/ProductManagePage.test.tsx`
- `src/admin/pages/products/ProductReviewPage.test.tsx`
- `src/admin/pages/orders/OrderManagePage.test.tsx`
- `src/admin/pages/users/UserManagePage.test.tsx`
- `src/admin/pages/reports/ReportManagePage.test.tsx`
- `src/admin/pages/reviews/ReviewManagePage.test.tsx`

- [ ] **Step 4: 验证 TypeScript 编译**

Run: `cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend && npx tsc --noEmit --pretty 2>&1 | head -40`
Expected: 无 PageData 相关编译错误

- [ ] **Step 5: 运行测试确保不报错**

Run: `cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend && npx vitest run --reporter=verbose src/admin/pages/ 2>&1 | tail -30`
Expected: admin 页面测试全部通过

- [ ] **Step 6: Commit**

```bash
cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend
git add src/admin/types/admin.ts src/admin/api/adminApi.ts src/admin/pages/products/ProductManagePage.test.tsx src/admin/pages/products/ProductReviewPage.test.tsx src/admin/pages/orders/OrderManagePage.test.tsx src/admin/pages/users/UserManagePage.test.tsx src/admin/pages/reports/ReportManagePage.test.tsx src/admin/pages/reviews/ReviewManagePage.test.tsx
git commit -m "refactor: unify pagination types - remove PageData, use PageResult globally"
```

---

### Task 3: AdminTable — 改用 shadcn Pagination 组件

**Files:**
- Modify: `src/admin/components/AdminTable.tsx`

- [ ] **Step 1: 替换 AdminTable 内联分页为 shadcn Pagination**

在 `src/admin/components/AdminTable.tsx` 中：

1. 保持 `pageButtonClass` CSS 常量（用于替换 shadcn Pagination 的样式以匹配 AdminTable 设计）
2. 替换第 239-330 行的内联分页渲染

当前内联分页逻辑（lines 239-330）：
```tsx
{pagination && totalPages > 1 && (
    <div className="relative flex items-center justify-between px-5 py-[0.9rem] border-t border-[#EDE8E3]">
        ...
        <div className="text-[0.81rem] text-[#B5AEA8]">
            共 <strong className="text-[#4A4540] font-semibold">{pagination.total.toLocaleString()}</strong> ... 条记录
        </div>
        <div className="flex items-center gap-[0.35rem]">
            <Button ...> <  ... </Button>  // 上一页
            {pageNumbers.map(...)}               // 页码按钮
            <Button ...> > ... </Button>     // 下一页
        </div>
    </div>
)}
```

改为：

```tsx
{pagination && totalPages > 1 && (
    <div className="relative flex items-center justify-between px-5 py-[0.9rem] border-t border-[#EDE8E3]">
        <div
            className="absolute top-0 left-5 right-5 h-px"
            style={{
                background: 'linear-gradient(90deg, transparent, rgba(249,115,22,0.08), transparent)',
            }}
        />

        <div className="text-[0.81rem] text-[#B5AEA8]">
            共 <strong className="text-[#4A4540] font-semibold">{pagination.total.toLocaleString()}</strong>{' '}
            条记录， 第 <strong className="text-[#4A4540] font-semibold">{pagination.current}</strong> /{' '}
            <strong className="text-[#4A4540] font-semibold">{totalPages}</strong> 页
        </div>

        <Pagination className="w-auto">
            <PaginationContent>
                <PaginationItem>
                    <Button
                        variant="ghost"
                        size="icon"
                        disabled={pagination.current <= 1}
                        onClick={() => pagination.onChange(pagination.current - 1)}
                        className={cn(
                            pageButtonClass,
                            pagination.current <= 1 && 'opacity-40 cursor-not-allowed'
                        )}
                    >
                        <ChevronLeft className="h-3.5 w-3.5" />
                    </Button>
                </PaginationItem>

                {pageNumbers.map((page, index) => {
                    if (page === 'ellipsis') {
                        return (
                            <PaginationItem key={`e-${index}`}>
                                <PaginationEllipsis className="text-[0.81rem] text-[#B5AEA8]" />
                            </PaginationItem>
                        );
                    }
                    return (
                        <PaginationItem key={page}>
                            <Button
                                variant="ghost"
                                size="icon"
                                onClick={() => pagination.onChange(page)}
                                className={cn(
                                    pageButtonClass,
                                    page === pagination.current &&
                                        'text-white bg-[linear-gradient(135deg,#F97316,#EA580C)] shadow-[0_2px_8px_rgba(249,115,22,0.28)]'
                                )}
                            >
                                {page}
                            </Button>
                        </PaginationItem>
                    );
                })}

                <PaginationItem>
                    <Button
                        variant="ghost"
                        size="icon"
                        disabled={pagination.current >= totalPages}
                        onClick={() => pagination.onChange(pagination.current + 1)}
                        className={cn(
                            pageButtonClass,
                            pagination.current >= totalPages && 'opacity-40 cursor-not-allowed'
                        )}
                    >
                        <ChevronRight className="h-3.5 w-3.5" />
                    </Button>
                </PaginationItem>
            </PaginationContent>
        </Pagination>
    </div>
)}
```

同时更新 import：
- 移除旧的箭头 SVG（`<path d="M15 ..."` 等），因为已用 `ChevronLeft`/`ChevronRight` 替代
- 添加 import：
  ```ts
  import { ChevronLeft, ChevronRight } from 'lucide-react';
  import {
      Pagination,
      PaginationContent,
      PaginationEllipsis,
      PaginationItem,
  } from '@/components/ui/pagination';
  ```

- [ ] **Step 2: 删除未使用的 `totalPages` 变量和 `useMemo`/`useState`**

注意：`totalPages` 在第 90 行计算（`const totalPages = pagination ? Math.ceil(pagination.total / pagination.pageSize) : 0;`），`pageNumbers` 在第 92-117 行使用 `useMemo` 计算。这些仍然需要保留，因为用在分页渲染中。`useState` 也是因为 `sortState` 需要保留。

所以不需要删除，只需确保 import 正确。

- [ ] **Step 3: 验证编译**

Run: `cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend && npx tsc --noEmit --pretty 2>&1 | head -20`
Expected: 无编译错误

- [ ] **Step 4: Commit**

```bash
cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend
git add src/admin/components/AdminTable.tsx src/components/ui/pagination.tsx src/components/ui/index.ts
git commit -m "feat: add shadcn Pagination component and refactor AdminTable to use it"
```

---

### Task 4: NotificationsPage — 迁移到手写分页 → shadcn Pagination

**Files:**
- Modify: `src/pages/notifications/NotificationsPage.tsx`
- Modify: `src/pages/notifications/notifications.css`

- [ ] **Step 1: 替换分页 UI**

在 `NotificationsPage.tsx` 中：

1. 替换第 236-260 行的手写分页代码：
```tsx
{totalPages > 1 && (
    <div className="notifications-pagination">
        <Button
            variant="outline"
            size="sm"
            className="pagination-btn"
            disabled={page <= 1}
            onClick={() => setPage(p => Math.max(1, p - 1))}
        >
            上一页
        </Button>
        <span className="pagination-info">
            {page} / {totalPages}
        </span>
        <Button
            variant="outline"
            size="sm"
            className="pagination-btn"
            disabled={page >= totalPages}
            onClick={() => setPage(p => p + 1)}
        >
            下一页
        </Button>
    </div>
)}
```

改为：

```tsx
{totalPages > 1 && (
    <Pagination className="notifications-pagination">
        <PaginationContent>
            <PaginationItem>
                <PaginationPrevious
                    onClick={() => setPage(p => Math.max(1, p - 1))}
                    className={cn(page <= 1 && 'pointer-events-none opacity-40')}
                />
            </PaginationItem>
            {Array.from({ length: totalPages }, (_, i) => i + 1).map(p => (
                <PaginationItem key={p}>
                    <PaginationLink
                        isActive={p === page}
                        onClick={() => setPage(p)}
                    >
                        {p}
                    </PaginationLink>
                </PaginationItem>
            ))}
            <PaginationItem>
                <PaginationNext
                    onClick={() => setPage(p => p + 1)}
                    className={cn(page >= totalPages && 'pointer-events-none opacity-40')}
                />
            </PaginationItem>
        </PaginationContent>
    </Pagination>
)}
```

2. 在文件顶部添加 import：
```ts
import { cn } from '@/lib/utils';
import {
    Pagination,
    PaginationContent,
    PaginationItem,
    PaginationLink,
    PaginationNext,
    PaginationPrevious,
} from '@/components/ui/pagination';
```

3. 删除未使用的 import（如果有 `ArrowLeft`, `ArrowRight` 不再需要 — 注意 `ArrowLeft` 在 header 返回按钮还在用，保留）

- [ ] **Step 2: 删除 CSS**

在 `notifications.css` 中删除 `.notifications-pagination`、`.pagination-btn`、`.pagination-info` 相关规则（lines 453-493）。

- [ ] **Step 3: 验证编译**

Run: `cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend && npx tsc --noEmit --pretty 2>&1 | head -20`
Expected: 无编译错误

- [ ] **Step 4: Commit**

```bash
cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend
git add src/pages/notifications/NotificationsPage.tsx src/pages/notifications/notifications.css
git commit -m "refactor: migrate NotificationsPage pagination to shadcn Pagination"
```

---

### Task 5: FavoritesPage — 迁移到 shadcn Pagination

**Files:**
- Modify: `src/pages/favorites/FavoritesPage.tsx`
- Modify: `src/pages/favorites/favorites.css`

- [ ] **Step 1: 替换分页 UI**

在 `FavoritesPage.tsx` 中：

1. 替换第 497-519 行的手写分页：
```tsx
{totalPages > 1 && (
    <div className="favorites-pagination">
        <Button
            variant="outline"
            className="pagination-btn"
            onClick={() => setPageNum(p => Math.max(1, p - 1))}
            disabled={pageNum <= 1}
        >
            上一页
        </Button>
        <span className="pagination-info">
            {pageNum} / {totalPages}
        </span>
        <Button
            variant="outline"
            className="pagination-btn"
            onClick={() => setPageNum(p => Math.min(totalPages, p + 1))}
            disabled={pageNum >= totalPages}
        >
            下一页
        </Button>
    </div>
)}
```

改为：

```tsx
{totalPages > 1 && (
    <Pagination className="favorites-pagination">
        <PaginationContent>
            <PaginationItem>
                <PaginationPrevious
                    onClick={() => setPageNum(p => Math.max(1, p - 1))}
                    className={cn(pageNum <= 1 && 'pointer-events-none opacity-40')}
                />
            </PaginationItem>
            {Array.from({ length: totalPages }, (_, i) => i + 1).map(p => (
                <PaginationItem key={p}>
                    <PaginationLink
                        isActive={p === pageNum}
                        onClick={() => setPageNum(p)}
                    >
                        {p}
                    </PaginationLink>
                </PaginationItem>
            ))}
            <PaginationItem>
                <PaginationNext
                    onClick={() => setPageNum(p => p + 1)}
                    className={cn(pageNum >= totalPages && 'pointer-events-none opacity-40')}
                />
            </PaginationItem>
        </PaginationContent>
    </Pagination>
)}
```

2. 在文件顶部添加 import：
```ts
import { cn } from '@/lib/utils';
import {
    Pagination,
    PaginationContent,
    PaginationItem,
    PaginationLink,
    PaginationNext,
    PaginationPrevious,
} from '@/components/ui/pagination';
```

- [ ] **Step 2: 删除 CSS**

在 `favorites.css` 中删除 `.favorites-pagination` 及其子规则（lines 959-997）。

- [ ] **Step 3: 验证编译**

Run: `cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend && npx tsc --noEmit --pretty 2>&1 | head -20`
Expected: 无编译错误

- [ ] **Step 4: Commit**

```bash
cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend
git add src/pages/favorites/FavoritesPage.tsx src/pages/favorites/favorites.css
git commit -m "refactor: migrate FavoritesPage pagination to shadcn Pagination"
```

---

### Task 6: OrdersPage — 添加分页

**Files:**
- Modify: `src/pages/orders/OrdersPage.tsx`

- [ ] **Step 1: 添加分页状态 + 分页 UI**

在 `OrdersPage.tsx` 中：

1. 添加分页状态（现有 `activeTab` 之后）：
```ts
const [pageNum, setPageNum] = useState(1);
const pageSize = 10;
```

2. 在 `queryParams` 中添加分页参数：
```ts
const queryParams = useMemo(() => {
    const tab = STATUS_TAB_MAP.find(t => t.id === activeTab);
    const baseParams: { status?: number; pageNum?: number; pageSize?: number } = {};
    if (tab?.statusCode !== undefined) {
        baseParams.status = tab.statusCode;
    }
    return {
        ...baseParams,
        pageNum,
        pageSize,
    };
}, [activeTab, pageNum]);
```

3. 添加 `totalPages` 计算（在 `orders` 解构之后）：
```ts
const totalPages = data?.pages ?? 1;
```

4. 在切换 tab 时重置 pageNum 到 1。修改 `handleTabChange`（如果存在）或在 `activeTab` 的 `setActiveTab` 调用处加 `setPageNum(1)`。

找到 tab 点击的渲染代码，在 `setActiveTab(tab.id)` 的同时添加 `setPageNum(1)`。

5. 在订单列表底部添加分页控件（在所有卡片渲染之后，`</div>` 闭合之前）：
```tsx
{totalPages > 1 && (
    <Pagination className="mt-6">
        <PaginationContent>
            <PaginationItem>
                <PaginationPrevious
                    onClick={() => setPageNum(p => Math.max(1, p - 1))}
                    className={cn(pageNum <= 1 && 'pointer-events-none opacity-40')}
                />
            </PaginationItem>
            {Array.from({ length: totalPages }, (_, i) => i + 1).map(p => (
                <PaginationItem key={p}>
                    <PaginationLink
                        isActive={p === pageNum}
                        onClick={() => setPageNum(p)}
                    >
                        {p}
                    </PaginationLink>
                </PaginationItem>
            ))}
            <PaginationItem>
                <PaginationNext
                    onClick={() => setPageNum(p => p + 1)}
                    className={cn(pageNum >= totalPages && 'pointer-events-none opacity-40')}
                />
            </PaginationItem>
        </PaginationContent>
    </Pagination>
)}
```

6. 添加 import：
```ts
import { cn } from '@/lib/utils';
import {
    Pagination,
    PaginationContent,
    PaginationItem,
    PaginationLink,
    PaginationNext,
    PaginationPrevious,
} from '@/components/ui/pagination';
```

- [ ] **Step 2: 验证编译**

Run: `cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend && npx tsc --noEmit --pretty 2>&1 | head -20`
Expected: 无编译错误

- [ ] **Step 3: Commit**

```bash
cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend
git add src/pages/orders/OrdersPage.tsx
git commit -m "feat: add pagination to OrdersPage"
```

---

### Task 7: MyProductsPage — 添加分页

**Files:**
- Modify: `src/pages/products/MyProductsPage.tsx`

- [ ] **Step 1: 替换硬编码分页为状态驱动+分页 UI**

在 `MyProductsPage.tsx` 中：

1. 添加分页状态：
```ts
const [pageNum, setPageNum] = useState(1);
const pageSize = 10;
```

2. 修改 `queryParams`：
```ts
const queryParams = useMemo(() => {
    const tab = STATUS_OPTIONS.find(t => t.id === activeTab);
    const code = tab?.status
        ? Number(Object.entries(PRODUCT_STATUS_CODE).find(([, v]) => v === tab.status)?.[0])
        : undefined;
    return {
        pageNum,
        pageSize,
        ...(code !== undefined ? { status: code } : {}),
    };
}, [activeTab, pageNum]);
```

3. 在 tab 切换时重置 pageNum：
找到 `setActiveTab(tab.id)` 的位置，改为 `setActiveTab(tab.id)` 后加 `setPageNum(1)`。或者在同一个事件处理中：
```tsx
onClick={() => {
    setActiveTab(tab.id);
    setPageNum(1);
}}
```

4. 添加 `totalPages`：
```ts
const totalPages = data?.pages ?? 1;
```

5. 在商品卡片列表底部添加分页 UI（在所有卡片渲染的 div 之后，但仍在主容器闭合之前）：
```tsx
{totalPages > 1 && (
    <Pagination className="mt-6">
        <PaginationContent>
            <PaginationItem>
                <PaginationPrevious
                    onClick={() => setPageNum(p => Math.max(1, p - 1))}
                    className={cn(pageNum <= 1 && 'pointer-events-none opacity-40')}
                />
            </PaginationItem>
            {Array.from({ length: totalPages }, (_, i) => i + 1).map(p => (
                <PaginationItem key={p}>
                    <PaginationLink
                        isActive={p === pageNum}
                        onClick={() => setPageNum(p)}
                    >
                        {p}
                    </PaginationLink>
                </PaginationItem>
            ))}
            <PaginationItem>
                <PaginationNext
                    onClick={() => setPageNum(p => p + 1)}
                    className={cn(pageNum >= totalPages && 'pointer-events-none opacity-40')}
                />
            </PaginationItem>
        </PaginationContent>
    </Pagination>
)}
```

6. 添加 import：
```ts
import { cn } from '@/lib/utils';
import {
    Pagination,
    PaginationContent,
    PaginationItem,
    PaginationLink,
    PaginationNext,
    PaginationPrevious,
} from '@/components/ui/pagination';
```

- [ ] **Step 2: 验证编译**

Run: `cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend && npx tsc --noEmit --pretty 2>&1 | head -20`
Expected: 无编译错误

- [ ] **Step 3: Commit**

```bash
cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend
git add src/pages/products/MyProductsPage.tsx
git commit -m "feat: add pagination to MyProductsPage (replaces hardcoded pageNum=1)"
```

---

### Task 8: MessagesPage — 添加分页

**Files:**
- Modify: `src/api/messageApi.ts`
- Modify: `src/pages/messages/MessagesPage.tsx`

- [ ] **Step 1: 修改 API 添加分页参数**

在 `src/api/messageApi.ts` 中：

```ts
// 将 getMessages() 改为接收分页参数
getMessages(pageNum = 1, pageSize = 20) {
    return request<PageResult<Record<string, unknown>>>('/messages/list', {
        method: 'GET',
        params: { pageNum, pageSize },
    });
},
```

更新 import 添加 `PageResult`：
```ts
import type { ChatSession, PageResult, RawChatMessage } from '@/types';
```

- [ ] **Step 2: 在 MessagesPage 中添加分页**

MessagesPage 目前展示的是会话列表（对话列表），不是消息分页。但后端 `GET /messages/list` 返回的是 `PageResult`。

`MessagesPage.tsx` 使用 `getConversations()` 获取会话列表。需要确认：消息列表分页是指会话列表分页还是消息历史分页？

从当前代码看，`MessagesPage` 展示会话列表，不涉及 `/messages/list` 分页。而 `getConversations()` 返回 `ChatSession[]`（非 PageResult）。

因此，MessagesPage 的分页问题实质上需要在消息列表展示中增加 `/messages/list` 的调用和分页。但观察当前 MessagesPage 的 UI 设计，它是一个聊天列表 + 聊天内容界面，分页方式应与当前设计保持一致。

最小改动方案：确认 `MessagesPage` 是否需要分页。如果仅展示活跃会话列表，当前可能不需要分页。但如果用户想查看历史消息，需要 `getMessages()` 支持分页。

参考设计文档：MessagesPage 需要加分页。建议在消息列表区域添加「加载更多」按钮或分页控件。

但为了最小化改动且符合设计统一性，建议在消息列表底部添加 shadcn Pagination（如果 `getMessages` 返回分页数据）。

- [ ] **Step 2a: （备选）给消息页面添加分页**

如果消息列表需要翻页，在 `MessagesPage.tsx` 中添加：
```ts
const [messagePage, setMessagePage] = useState(1);
const messagePageSize = 20;

const { data: messagesData } = useQuery({
    queryKey: ['messages', 'list', messagePage],
    queryFn: async () => {
        const response = await messageApi.getMessages(messagePage, messagePageSize);
        return response.data;
    },
    staleTime: 15 * 1000,
});

const totalPages = messagesData?.pages ?? 1;
```

并在渲染区域添加分页 UI。

但鉴于 `MessagesPage` 是聊天 UI（包含会话列表和聊天窗），分页方式可能与常规列表不同。建议先确认消息页面的具体分页需求。

> **注意：** 由于 `MessagesPage` 结构复杂（聊天 UI），分页方式需与当前设计匹配。如果当前仅显示活跃会话，可能不需要改动。如果确认需要消息历史分页，建议以「加载更多」按钮形式实现，而非传统分页按钮。

- [ ] **Step 3: Commit API 修改**

```bash
cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend
git add src/api/messageApi.ts
git commit -m "feat: add pagination params to messageApi.getMessages()"
```

---

### Task 9: SemanticSearchResult — 补齐 pages 字段

**Files:**
- Modify: `src/api/aiApi.ts`

- [ ] **Step 1: 添加 `pages` 字段**

在 `src/api/aiApi.ts` 中：

```ts
export interface SemanticSearchResult {
    records: RawProduct[];
    total: number;
    current: number;
    size: number;
    pages: number;  // 新增：与 PageResult 结构一致
}
```

- [ ] **Step 2: 更新使用方确保 `pages` 被计算**

在 `src/hooks/useSemanticSearch.ts` 中，当收到语义搜索结果时，计算 `pages`：

```ts
const response = await aiApi.semanticSearch({ keyword, pageNum, pageSize });
const rawRecords = response.data?.records ?? [];
const total = response.data?.total ?? 0;
const size = response.data?.size ?? pageSize;
// 补齐 pages 字段（后端可能不返回）
const normalized = rawRecords.map(r => normalizeProduct(r));
setResults(normalized);
setTotal(total);
// pages 字段在响应中可能缺失，需要计算
```

实际上，由于前端接口定义已经声明 `pages: number`，我们只需确保在语义搜索 API 返回时做兜底。最简单的方案是在 API 层或 hook 中计算 `Math.ceil(total / size)` 作为 `pages`。

但更好的做法是在 `aiApi.ts` 的 `semanticSearch` 函数中做转换：

```ts
async semanticSearch(params: ProductSearchParams & { keyword: string }) {
    const response = await request<SemanticSearchResult>('/ai/semantic-search', {
        method: 'POST',
        body: params,
    });
    // 补齐 pages 字段（后端 SemanticSearchResult 没有 pages）
    if (response.data && response.data.pages === undefined) {
        response.data.pages = Math.ceil(response.data.total / response.data.size);
    }
    return response;
},
```

或者更简单：在 `useSemanticSearch.ts` 中不要依赖 `pages`，因为语义搜索不涉及分页 UI（SearchPage 使用虚拟滚动）。

考虑到 SearchPage 使用虚拟滚动，`pages` 字段在语义搜索场景中实际上不会被使用。所以最小改动就是只在类型定义中添加 `pages` 字段，不做额外逻辑。

- [ ] **Step 3: Commit**

```bash
cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend
git add src/api/aiApi.ts
git commit -m "fix: add pages field to SemanticSearchResult for consistency with PageResult"
```

---

### Task 10: 验证 + 最终清理

- [ ] **Step 1: 运行完整 TypeScript 编译检查**

Run: `cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend && npx tsc --noEmit --pretty 2>&1 | tail -20`
Expected: 无编译错误

- [ ] **Step 2: 运行前端测试**

Run: `cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend && npm test 2>&1 | tail -30`
Expected: 现有测试全部通过

- [ ] **Step 3: 检查是否有遗漏的 PageData 引用**

Run: `cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend && grep -rn "PageData" src/ 2>&1`
Expected: 无匹配结果（全部替换为 PageResult）

- [ ] **Step 4: 确认 git 状态**

Run: `cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend && git status`

- [ ] **Step 5: 生成最终 commit 总结**

```bash
cd /home/cartethyia/projects/Java/easy-orange/easyorange-frontend
git log --oneline -10
```
