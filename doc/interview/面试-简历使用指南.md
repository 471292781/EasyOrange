# EasyOrange 面试 & 简历使用指南

> **用途**：给自己看的内部门档，决定「这个项目在简历上怎么写、面试时怎么讲」。对应岗位：AI Agent / 大模型 Java 后端、Java 架构师 / 高级后端、全栈通用。
>
> **配套文档**（按面试流程使用）：
> - [话术集.md](话术集.md) — 通用叙事 + 高频追问应答（「怎么讲」）
> - [核心代码走读指南.md](核心代码走读指南.md) — 13 个核心类的实现细节（被点名到类时「怎么答」）
> - [高频技术题速答.md](高频技术题速答.md) — 54 道八股题速答 + 项目结合点
> - [系统设计与手撕.md](系统设计与手撕.md) — 5 道设计题框架 + 8 道手撕代码参考实现
> - [模拟面试题库.md](模拟面试题库.md) — 两条岗位追问链 + 通用题库 + HR 行为面
> - [DDD-核心思想轻量落地.md](DDD-核心思想轻量落地.md) — DDD 专项清单
>
> 版本：v1.2 · 2026-08-08

---

## 一、岗位匹配矩阵（先选方向再看下面的内容）

| 你投的岗位 / JD 关键词 | 叙事主线占比 | 重点章节 | 次要章节 | 可以跳过 |
|---|---|---|---|---|
| **AI Agent / 大模型 Java 后端**（LLM 应用 / Agent 框架 / LangChain4j / Spring AI / RAG / Embedding / Prompt / 多模态） | 60% AI + 25% 架构 + 15% 业务 | README 双岗位适配面板 → 轻量级 Agent 编排 → AI 工程化 8 项 → 拒绝项（LangChain4j、Milvus）→ 工程指标 §4.1 | 下单一致性（拒绝 Saga）→ Outbox/DLQ → 30s 钩子 | message 模块 WS、前端工程化 |
| **Java 架构师 / 高级后端**（DDD / Saga / 分布式事务 / CQRS / 事件驱动 / 高可用 / 限流熔断 / 中间件选型 / 架构治理） | 20% AI + 65% 架构 + 15% 业务 | README 架构治理三板斧 → 拒绝项（2PC、Seata、全模块 CQRS）→ 下单一致性（拒绝 Saga）+ Outbox + traceId → 工程指标 §4.2 | AI 8 件套 → PIT 变异测试 | 具体 6 个 AI 决策点的业务细节 |
| **通用 Java 后端**（大厂春招 / 秋招 / 实习，JD 没明确方向） | 40% AI + 40% 架构 + 20% 业务 | 30s 钩子 → 1min 自我介绍 → 选一个你最熟的故事（AI 或下单一致性 / 拒绝 Saga）深挖 → 工程指标 §4.0/4.3 | 另一个方向作为次亮点展示 | 高频追问的深层细节 |
| **全栈 / 前后端都问**（小厂 / 创业公司） | 25% AI + 25% 架构 + 50% 前后端联动 | README 技术栈 → 快速开始 → Admin 设计系统 + 组件化 → 模块总览 | 任意一侧主线 + 前端 a11y / React Query 客户端状态 | 变异测试、traceId 深层链路 |

> **原则**：一个主线故事讲到底 + 次方向作为补充亮点抛出来，不要两个方向对半开（导致两边都讲不深）。

---

## 二、简历上怎么写（两套可直接用的版本）

> 位置：「项目经历」或「个人项目」。控制在 4-6 个 bullet，每个 bullet 以「动词 + 量化结果 + 关键技术」结构。

### 2.1 🎯 版本 A：AI Agent / 大模型 Java 后端方向

```
【项目】EasyOrange — LLM × DDD 工程化实战（个人开源，Gitee ✨xxx star）
• 设计并实现 6 个 AI 决策点（智能估值/文案/找货/评估/信用画像/审核）全链路生产级工程化，全面框架化为 Spring AI 2.0（ChatModel/EmbeddingModel bean + Embedding 真实现 kNN）+ Redisson 令牌桶限流 + stale 缓存降级 + Prompt YAML 版本化 + @TokenBudget AOP 日预算治理，形成完整 8 件套（ADR-0008）
• 手写轻量级 Agent Planner（120 行），基于 Spring AI ChatModel 编排 + CompletableFuture 虚拟线程 4 路并行 Tool Calling（LLM 意图识别路由 + 商品标签提取 + 市场分析 + 建议问题生成），编排手写不依赖 LangChain4j 反射黑盒，面试可拆解每层实现细节
• DDD 六边形架构落地 11 Maven 模块 + 35 Port 接口编译期隔离，ArchUnit 10 条规则 CI 阻断违规依赖；下单链路本地单事务 + Redisson 分布式锁防超卖 + Spring Modulith Outbox + DLQ 三级重试保障事件驱动可靠性（拒绝 Saga，ADR-0007）
• 2,412 测试用例（后端 1,356 + 前端 1,056），Domain 层 JaCoCo 84.1% 行覆盖 + 71.5% 分支覆盖，PIT 变异测试 order 模块 mutation 70% / test strength 89% 验证测试真实缺陷检测能力
• 技术栈：Java 25 · Spring Boot 4 · DeepSeek / 通义千问 VL · Redis 7.4 · RabbitMQ 3.13 · MySQL 8.4 · Elasticsearch 8
```

### 2.2 🏛 版本 B：Java 架构师 / 高级后端方向

```
【项目】EasyOrange — LLM × DDD：Java 架构工程化实战（个人开源，Gitee ✨xxx star）
• 采用 DDD 六边形 + 分层架构拆分为 11 Maven 模块 + 35 Port 接口编译期隔离，9 条 ADR（架构决策记录）记录选型逻辑 + 拒绝项（2PC/XA/Seata/TCC、Saga 编排、全模块 CQRS、LangChain4j、Milvus、Kafka/Pulsar），ArchUnit 10 条规则在 CI 阶段阻断违规依赖
• 下单链路采用本地单事务 + Redisson 分布式锁 + Outbox（拒绝 Saga，ADR-0007）：OrderCommandHandler 单一 @Transactional 内按 productId 排序加锁防死锁防超卖 → 建单 + 发布事件（Outbox 同事务原子）→ 同步扣库存 → 建支付，失败整体回滚、无补偿路径；取消/退款恢复库存、完成标记售出由 OrderLifecycleEventConsumer 异步触发（拒绝 Seata AT/TCC/XA 的 4 点理由，ADR-0001 Superseded）
• 事件驱动可靠性三层保障：Spring Modulith Outbox（业务表 + EVENT_PUBLICATION 同事务原子写入，崩溃自动重发）→ DLQ 三级重试（DlqRetryScheduler 每 5min + 指数退避 1/5/15min + terminal 毒消息转储）→ EventIdempotencyChecker 幂等去重；全链路 traceId（HTTP → MDC → @Async → RabbitMQ header → 消费者 MDC）
• LLM 集成工程化 8 件套：Spring AI 2.0 框架化（ChatModel/EmbeddingModel + Embedding 真实现）+ Redisson 令牌桶限流 + stale 降级 + Prompt YAML + @TokenBudget + 多模态 Vision，6 个 AI 决策点覆盖业务两端，验证 AI 在复杂架构体系中的落地可行性
• 2,412 测试（后端 1,356 + 前端 1,056），Domain 层 JaCoCo 84.1%，PIT 变异测试验证测试真实检测能力；OWASP Dependency-Check CVSS≥8 阻断构建；完整 GitHub Actions CI（build-and-test + security-scan）
• 技术栈：Java 25 · Spring Boot 4 · MyBatis-Plus 3.5 · Spring Modulith · Resilience4j · Redisson · RabbitMQ 3.13 · Redis 7.4 · MySQL 8.4 · Elasticsearch 8 · Flyway 11
```

> **Star 数量**：Gitee 没 star 就删掉括号，或写「开发中」。面试官一般不会当场搜 star 数量，重点是 bullet 的内容质量。

---

## 三、自我介绍话术（三档时间版本）

> 原则：**开场抛钩子 → 主线讲故事 → 留一个可追问的点**。不要机械背数字。用「我做了一个项目叫 EasyOrange，定位是___，核心矛盾是___，我的解法是___，结果是___」结构。

### 3.1 30 秒版（自我介绍收尾，或 HR 电话面）

> 我做了一个开源项目 EasyOrange，定位 **LLM × DDD：Java 架构工程化实战**——一句话就是在 DDD 六边形架构里装 LLM，让 AI 链路可换供应商、可降级、可观测。一套代码双岗位适配：投 AI Agent 看 6 决策点 + 手写轻量 Agent 编排 + 8 件套工程化；投架构师看 11 模块 + 拒绝 Saga 的 ADR 决策（本地单事务 + 分布式锁 + Outbox）+ ADR/ArchUnit/PIT 治理三板斧。**2,412 测试、Domain 层 84.1% 行覆盖 + PIT 变异测试双门禁**。

### 3.2 1 分钟版（技术面开场自我介绍）

> 根据你选的方向直接读 [工程指标.md §4.1.1 或 §4.2.1](../工程指标.md)。背熟，讲的时候语速稍慢，碰到关键数字自然停顿一下（比如「**84.1% 行覆盖**」、「**PIT mutation 70%**」），让对方有机会插话追问。

### 3.3 3 分钟版（深度面 / 让你主动讲项目细节时）

> 1min 版本 + 展开一个「被拒绝方案」的对比——比如 AI 方向展开「为什么不用 LangChain4j 自己手写 Tool Registry」；架构师方向展开「为什么单库下单不用 Saga（拒绝 Saga 的决策）」+ Seata AT/TCC 技术对比。讲完停一下，抛一句「这块要不要我再展开某个细节？」把节奏交给面试官。

---

## 四、叙事节奏建议（面试现场怎么切到 EasyOrange）

### 场景 1：面试官问「介绍一下你做过的项目」

**不要**：按时间顺序从实习第一个项目讲到现在。**要**：先问「是想了解实习/工作项目，还是个人开源项目？我个人有个深度比较大的项目 EasyOrange，可能更能展示架构思考和 AI 工程化能力。」——引导面试官进入你的主战场。

### 场景 2：面试官问「你熟悉 Spring Boot / DDD / 分布式事务吗？」

**不要**：直接背定义。**要**：定义一句话带过，然后接「我在 EasyOrange 项目里实际落地的时候，遇到的问题是___，我是这么处理的___。」把问题从「背诵题」变成「实战展示题」。

### 场景 3：面试官问「说一下 Redis / RabbitMQ / Resilience4j 怎么用」

**不要**：讲语法（`opsForValue().set` 这种）。**要**：讲场景+取舍——「Redis 在我项目里有 4 种用法：①L2 缓存配合 Caffeine L1；②Redisson 令牌桶 + RRateLimiter 替代固定窗口（解决边界突刺）；③EventIdempotencyChecker 幂等去重 SETNX；④Pub/Sub 广播 L1 失效。踩过的坑是 Spring Boot 4 默认 RedisTemplate 不设序列化器，Lua tonumber 返回 nil 导致限流器 fail-open，后来我们统一 RedisConfig 设 GenericJacksonJsonRedisSerializer 解决了。」

---

## 五、知识盲区标注（面试前必须复习或提前想好「怎么绕」）

> ⚠️ 这是**最重要的一节**。下面这些点是文档叙事里提到但真实项目可能没做到 100% 的，面试官追问时别翻车。

| 追问方向 | 叙事里提到的 | 实际项目需要确认 | 回答不上时的「绕法」 |
|---|---|---|---|
| **LangChain4j / Spring AI 源码细节** | 「拒绝了 LangChain4j」 | 你是不是真的读过这俩框架源码？Tool 反射调用是怎么走的？ | 老实答「我没有深入到 Spring AI 的 Bean 注册源码，但对比评估了它们的 Tool 抽象——LangChain4j 的 @Tool 是编译期生成 GPT JSON Schema，Spring AI 的 FunctionCallback 是运行时反射，都不如我自己写的 CompletableFuture 并行 Tool 灵活，调试也能直接在 AiSearchEnhancerAdapter 打断点。我倾向于在面试展示场景下自己实现，复杂度可控。」 |
| **Seata TC/TM/RM 部署模式** | 「拒绝了 Seata AT/TCC / Saga 编排」 | Seata 三种模式（AT/TCC/Saga）的区别？TC Server 高可用怎么做？ | 「我没有在生产环境跑过 Seata 集群，评估阶段主要看的是官方文档和社区踩坑帖——AT 模式的核心问题是 undo_log + 全局锁的性能开销（每个分支事务写完业务 SQL 还要写 undo_log，TC 协调全局锁），跟我们的本地单事务 + Outbox 事件驱动比，写路径成本和中间件依赖都高一截；而且我们的 @ConditionalOnProperty 降级模式要求 Seata TC 挂了也能起应用，TC 是单点做不到，所以就没深入部署细节。」 |
| **HNSW / IVF 向量索引原理** | 「拒绝了 Milvus/PGVector」 | HNSW 近邻搜索的 M / efConstruction 参数？余弦相似度和点积区别？ | 「我对向量检索原理主要是调研阶段的理解——HNSW 是分层可导航小世界图，M 控制每层邻居数，efConstruction 控制建图探索宽度。我们现在 SKU < 10 万，Top 50 BM25 结果再用 Java 原生 Cosine 做点积 rerank，P95 < 300ms 完全够用。真到了需要 HNSW 的规模，应该上专门的向量团队维护 Milvus，不是业务后端 Java 工程师维护 ANN 参数。」 |
| **PIT 变异测试全量耗时** | 「order 41 秒 / product 41 秒」 | 11 模块全量 PIT 要多久？CI 里跑得起吗？ | 「目前只对 domain 层注入变异，也只在 CI 的 nightly build 里跑，PR 只跑 JaCoCo + ArchUnit。product 模块 253 个变异 41 秒，order 81 个变异 41 秒——差距大是因为 product 有更复杂的状态机枚举转换，变异点位多。全量 domain 层 PIT 预估 3-5 分钟，nightly 时间窗口完全够。」 |
| **traceId 跨服务怎么传** | 「HTTP → MQ → MDC」 | 用户服务和订单服务如果拆成两个独立 JVM，traceId 怎么传？你们现在是单 JVM 模块吧？ | 「对，目前 EasyOrange 是单 JVM 多 Maven 模块部署，模块间事件驱动通过 RabbitMQ 走的是同一 Spring 上下文，所以 Brave TracingFilter 提取的 traceId 能通过 EventMetadataMessagePostProcessor 注入到 message header，消费者端再 decode 回 MDC。如果拆成多 JVM 微服务，就是在服务间的 HTTP 客户端加拦截器，把 traceId 放到 X-B3-TraceId 请求头里，走 Brave / OpenTelemetry 的标准 W3C traceparent，这套机制是通用的。」 |
| **@TokenBudget 超预算后怎么降级** | 「返回缓存最近一次结果 + 告警」 | 降级是全局开关还是按场景开关？告警推到哪里？ | 「目前 TokenBudgetExceededException 被 GlobalExceptionHandler 捕，降级逻辑是——查 Redis 缓存 `eo:ai:lastSuccess:{scope}:{userId}` 最近一次成功结果，没有的话返回 Result.fail(ResultCode.AI_BUDGET_EXCEEDED) + 业务侧提示「当前 AI 服务繁忙请稍后再试」。告警还在业务日志 WARN 级别，下一阶段打算接 Prometheus Alertmanager 推飞书 / 企微——这是故意留的 TODO，因为项目主要是展示 AI 工程化闭环，监控对接每个公司平台不一样，留接口更灵活。」 |
| **MySQL 8.4 到底用了哪些新特性** | 「MySQL 8.4 LTS」 | 8.4 跟 8.0 的区别？InnoDB 变更？ | 「项目里主要用的是 MySQL 8.x 系列的稳定能力——窗口函数在对账统计里用、CTE WITH 递归在商品分类树里用。选 8.4 是因为它是 Oracle 官方标注的 Innovation LTS，生命周期到 2032 年，比 8.0 的 EOL（2026 年 4 月）长。没有用 8.4 特有的新语法，主要是基于生命周期选型，避免刚上线就遇到 EOL 升级压力。」 |

> **通用绕法公式**：「我没有深入到 X 的源码/生产部署，但评估时我关注的关键点是___，最后做出的取舍是___，所以这块我就没深入到那个程度——如果真到了那个场景，我会按___的思路去做。」→ **诚实展示思考过程 > 硬背不知道的细节**。

---

## 六、下一步项目强化建议（如果还有 1-2 周时间）

> 按 ROI 从高到低排序，做了能加多少分、需要多少代码量。

| 优先级 | 项目 | 能加什么分 | 代码量 | 适合方向 |
|---|---|---|---|---|
| ✅ 已完成 | **ADR-0006 模块解耦**（[ADR-0006](../adr/0006-module-decoupling-port-adapter-acl.md)，2026-08-08） | 架构师方向 ADR 补齐断号（8 → 9 条），面试可讲「35 Port 编译期隔离 + optional + ArchUnit 双保险 + 拒绝共享内核」 | 零代码（纯 Markdown） | 架构师 |
| 🥈 P1 | **在 AiSearchEnhancerAdapter 上加一个 Tool 注册的 Interface**（`AiTool` 接口：`name / execute(request)`，4 个 Tool 分别实现） | AI Agent 方向把「4 路 CompletableFuture」升级成「Tool Registry + Planner」，能讲出 OpenAI Function Calling 的 JSON Schema 生成思路 | ~200 行 Java + 10 个单测 | AI Agent |
| 🥉 P1 | **给 eo_ai_call_log 表加 Flyway V2 迁移 + 定时任务做 LLM-as-Judge 离线 Eval** | 可观测 + 评估闭环，回答「怎么判断 AI 输出质量」这个必问问题 | ~150 行 SQL + Java + YAML | AI Agent |
| 🏅 P2 | **在 easyorange-ai 模块加 LangChain4j 作为「备选 Adapter」**——保留手写 Planner，同时加 `LangChain4jAiSearchAdapter`（用 @Tool 注解 + AiServices），@ConditionalOnProperty 切换 | 回答「为什么不用 LangChain4j」时可以直接展示代码：「我两个都实现了，对比后选择了手写，原因是___」 | ~300 行 + pom.xml 加依赖 | AI Agent |
| 🏅 P2 | **ADR-0007 演进的「真 Saga / 事务性 Outbox 补偿」方案**——把 product/payment 模块拆分独立数据源 / 独立部署，下单链路从本地单事务演进为跨服务编排（真 Saga）或事务性 Outbox 补偿 | 架构师方向能讲「什么时候才需要 Saga」的决策分界线：单库用本地事务 + 分布式锁 + Outbox，跨库/独立部署才引入编排，深度加一层 | ~300 行 Java + 模块拆分方案 | 架构师 |
| 🎗 P3 | **接入 NATS JetStream 做消息双写**——保留 RabbitMQ，新增 NATS 配置和 @ConditionalOnProperty 切换 | 对应上次「RabbitMQ 好不好」的讨论，能讲「云原生消息中间件选型对比」 | ~400 行 + pom.xml 加依赖 | 架构师 |
| 🎗 P3 | **前端加一个「AI Playground」页面**——输入自然语言，看 4 路 Tool Calling 的过程可视化（用流式 SSE 返回每个 Tool 的结果） | 展示 AI 调用可视化，面试官打开页面能直接看到效果，记忆点极强 | ~500 行 TSX + CSS + 后端加一个 SSE 端点 | 全栈 / AI Agent |

> **做完 P0 + 任一 P1 就能再升一个档次**。别贪多，P2/P3 都是加分项不是必须项。

---

## 七、面试流程 CheckList（面前 10 分钟过一遍）

- [ ] 确认今天投的岗位方向，对应选好 §一 的主线叙事（60%/25%/15% 比例）
- [ ] 把简历 bullet 对应到 README 的 4 个章节，做到「简历提一句话 → 能展开讲 1 分钟 + 回答 3 个追问」
- [ ] 过一遍 §五 知识盲区标注，确保每个点都能按公式绕过去
- [ ] 过一遍 [核心代码走读指南.md](核心代码走读指南.md) 的 13 个类（每类 5 分钟），被点名任何核心类都能讲出「实现要点 + 一句话回答」
- [ ] 过一遍 [模拟面试题库.md](模拟面试题库.md) 对应岗位的完整追问链（找朋友模拟或自问自答到第 3 层追问）
- [ ] 手撕题自测（[系统设计与手撕.md](系统设计与手撕.md) §四清单）——至少写熟令牌桶/状态机/幂等三题
- [ ] 想 1-2 个「被问倒了也能展示思考过程」的问题（比如「让你设计 10 倍流量这个项目怎么改？」→ 答「垂直拆 3 个域服务 + 独立数据源后引入真 Saga / 事务性 Outbox 补偿 + Redis Cluster + 读写分离 + PIT 变异测试跑 nightly」）
- [ ] 准备 1 个反问面试官的问题（不要问「公司技术栈是什么」，要问「你们团队现在的 AI 工程化是怎么处理 LLM 降级和成本控制的？」或「你们用 DDD 的话是怎么守 domain 层零框架依赖的？」——反向展示你对这个岗的深度理解）

---

## 八、话术速查表（面中卡壳时脑子里搜这个）

| 面试官问的关键词 | 跳转到的内容位置 |
|---|---|
| DDD / 六边形 / 分层 | README 架构治理三板斧 → AGENTS.md §后端架构核心原则 |
| Saga / 分布式事务 / Seata / TCC / 2PC | 工程指标 §4.2.1 下单一致性（拒绝 Saga）→ §4.2.2 拒绝 Saga / Seata 追问应答 → ADR-0007（ADR-0001 Superseded） |
| 消息队列可靠 / 幂等 / 死信 / Outbox | README 架构总览 单事务+锁+Outbox+traceId → 工程指标 §4.2.2 Outbox 应答 |
| 限流 / 熔断 / 降级 / Bulkhead | README AI 工程化 8 项 → 工程指标 §4.1.2 TokenBudget / §1.5 限流熔断 |
| LLM / LangChain4j / Spring AI / RAG / 向量 | README 轻量级 Agent 编排 → 工程指标 §4.1.2 LangChain4j / Milvus 追问应答 |
| Prompt 工程 / 多模态 / Vision / Tool Calling | README 拒绝项清单 → 工程指标 §4.1.2 Vision + Tool Calling |
| 测试 / 覆盖率 / JaCoCo / PIT 变异 | README 质量门禁 → 工程指标 §4.2.2 PIT 应答 |
| 架构治理 / 技术选型 / ADR | README 拒绝项清单 + 架构治理三板斧 |
| 业务复杂度 / 为什么做 C2C / 为什么不做电商 | 工程指标 §4.3 通用防守话术 |

---

祝面试顺利。有任何追问翻车的点记下来，回到这里补充 §五 盲区标注，下次就不翻了。
