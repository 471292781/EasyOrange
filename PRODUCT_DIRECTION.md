# EasyOrange — 让大模型在真实业务中稳定运行的全栈工程实践

> 调通 API 只是起点。缓存、限流、降级、可观测——让大模型在真实业务约束下稳定运行，才是 AI 工程化的核心命题。

> 版本：v3.0 · 适用：项目展示 / 比赛答辩 / 面试讲解
>
> **本文档定位**：说明项目所选的**业务场景**（即"业务载体"）的边界、用户、流程与 AI 能力清单。
> **本文档不是商业计划书**——业务场景的简化是为了让架构与工程本身成为主角。技术架构详见 [CLAUDE.md](./CLAUDE.md)、[AGENTS.md](./AGENTS.md) 与 [doc/架构/](./doc/架构/)。

> **项目定位**：EasyOrange 是一个 AI 工程化全栈项目 — 以 C2C 资产流转为业务载体，展示大模型从 API 调用到生产级服务的完整工程链路。
> **业务载体**：C2C 资产流转
> **业务简化原则**：业务场景被刻意简化（固定价格、C2C 直发、平台不碰货），把全部精力放在架构与工程上。

---

## 一、为什么选 C2C 资产流转作为业务载体

业务场景需要满足"展示架构深度"的几个必要条件：

| 必要条件 | 在 C2C 资产流转中的体现 |
|---|---|
| **复杂领域模型** | 资产 / 订单 / 支付 / 消息 / 收藏 / 信用，多聚合根、值对象、领域事件丰富 |
| **跨模块事务** | 订单创建 → 库存锁定 → 支付 → 发货 → 退款，Saga 模式天然落地 |
| **实时通信需求** | 聊天 / 通知 / 信用变化，WebSocket + STOMP 协议验证 |
| **AI 落地空间** | 智能估值 / 营销文案 / 智能找货 / 资产核验 / 信用画像，多模态集成充分 |
| **CQRS 适用面** | 资产读多写少 + 全文搜索聚合，命令/查询分离收益明显 |
| **跨团队协同** | 资产方 / 认领方 / 平台运营 / 管理员，端口-适配器隔离自然 |

> 选这个场景不是为了做商业，是为了在真实业务压力下展示架构与工程的成熟度。

### 「资产」的语义边界（主动定义）

EasyOrange 中的**资产**是广义概念，**不局限于二手实物**：

- **实物资产**：数码 3C、书籍教材、服饰鞋包、生活用品、运动装备
- **虚拟/数字资产**：视频/音乐会员、游戏账号与皮肤、云服务订阅、创作素材与模板
- **权益类资产**：健身卡转卡、自习室时长、课程兑换码、线下活动名额
- **时间/技能资产**：可转让的服务预约、技能咨询时段（架构可承载）

**主动区隔**：这不是"闲鱼/转转"式的二手交易平台，而是**个人资产管理与流转网络**。平台不碰货、不议价、不持有库存——资产方按固定价格上架，AI 仅在两端做辅助决策。听众联想到二手交易时，需要被明确告知：这里的"资产"包含你手机里那份没用完的会员、硬盘里那套设计素材，而不只是衣柜里的旧衣服。

---

## 二、业务场景的边界（刻意简化）

为了把精力放在架构与工程上，业务场景做了如下简化：

| 维度 | 简化决策 | 原因 |
|---|---|---|
| 价格机制 | **固定价格** | 议价 / 阶梯降价会带来复杂状态机，侵蚀架构主线 |
| 物流模式 | **C2C 直发** | 平台不碰货、不囤货，物流走资产方→认领方 |
| 资金流 | **不经手资金** | 避免支付牌照、备付金等合规复杂度 |
| 商业模式 | **陪跑，不深入** | 业务不是主角，架构才是 |
| AI 角色 | **辅助决策** | AI 替资产方"运营"的营销叙事被弱化；技术层面保留全部 AI 集成 |

> 简化后的业务场景，留出全部精力给架构和工程。架构师能讲清楚 DDD / CQRS / Saga / 事件驱动 / AI 集成的细节，比业务复杂度本身更有展示价值。

---

## 三、用户角色与场景

### 资产方（asset submitter）

想让闲置资产**流转起来**的人，关心的是"别让它闲着"。资产可以是仓库里的旧手机，也可以是账户里剩半年的视频会员，或是那套只用了一次的设计模板。

**典型动作**：
1. 拍几张图（实物）/ 截图凭证（虚拟）
2. 选分类 + 写标题描述（AI 可帮写 4 种风格文案）
3. 设固定价格 + 让 AI 给出建议售价
4. 上架 → 等待认领方浏览 / 主动咨询
5. 成交 → 自行交割（实物发货 / 虚拟移交）→ 收款

### 认领方（claimant）

想以合理价格**领走资产**的人，关心的是"找得到、看得清、领得放心"。无论是收一台二手笔记本，还是接手一份还剩三个月的健身卡。

**典型动作**：浏览 → AI 智能找货 → AI 资产核验（实物拍照验货 / 虚拟凭证核查）→ 看清 AI 信用画像 → 下单

### 平台运营 / 管理员

通过管理端处理用户、商品、订单、分类、举报等。

---

## 四、AI 能力清单（架构展示为主）

### 资产方侧 3 个能力

1. **AI 资产估值** — 基于历史成交 + 实时市场 + 商品成色 + 用户信用，给出建议售价
2. **AI 营销文案** — 自动生成 4 风格标题与描述
3. **AI 信用画像（资产方）** — 5 维雷达图（描述准确度 / 沟通及时度 / 发货速度 / 售后口碑 / 历史评价）

### 认领方侧 3 个能力

4. **AI 智能找货** — 自然语言搜货（"想要 500 以内的桌面摆件" → 精准匹配）
5. **AI 物品评估** — 拍照生成"看货清单"，识别可能瑕疵、描述与实物差异
6. **AI 信用画像（认领方）** — 5 维雷达图

### 架构侧关注点

- LLM / Vision 的端口抽象（`LlmPort` / `VisionPort`）——支持实物图片识别与虚拟资产文本解析
- 多级缓存装饰器（`CachingLlmAdapter` `@Primary`）
- 限流与降级（`AiRateLimitInterceptor`）
- 缓存键设计与命中率优化

> 详细机制见 [doc/集成/AI-资产管理.md](doc/集成/AI-资产管理.md)。

---

## 五、技术架构（工程深度才是主角）

### 12 Maven 模块（hexagonal / DDD / CQRS / 事件驱动）

```
application        ← 启动/装配/外部契约
  ├── ai           ← DeepSeek + 通义千问 VL 适配（Port/Adapter）
  ├── product      ← Product 聚合根 + 审核工作流 + 全文搜索
  ├── order        ← Order 聚合根 + Saga 补偿
  ├── payment      ← 支付通道
  ├── user         ← User + 信用评分
  ├── favorite     ← 收藏（六边形架构）
  ├── message      ← 站内信 + WebSocket 实时消息
  ├── admin        ← 管理端 API
  ├── framework    ← Security + Redis + 多级缓存 + Bloom + RabbitMQ
  └── common       ← 通用基础设施
```

### 关键架构亮点

- **DDD 聚合根 + 领域事件**：Product / Order 各为独立聚合根
- **CQRS 读写分离**：查询侧走独立 ReadModel + 搜索聚合
- **端口-适配器隔离**：`SellerInfoPort` / `UserInfoPort` / `ProductInventoryPort` 等
- **RabbitMQ 事件驱动**：9 个消费者队列 + DLQ + 指数退避重试
- **WebSocket + STOMP**：实时聊天 / 通知 / 在线状态
- **Saga 补偿**：订单超时 → 自动取消 → 商品重新可售
- **AI 多模态集成**：LLM 文本 + Vision 视觉 + 限流 + 多级缓存

---

## 六、面向比赛 / 面试的讲解框架

### 推荐讲解顺序

> **业务载体**：C2C 资产流转（复杂领域模型 + 跨模块事务 + 实时通信 + AI 落地）
> **架构主线**：DDD 11 模块 → CQRS 读写分离 → Saga 分布式事务 → 事件驱动 → AI 多模态
> **工程深度**：1,269 后端测试用例 + 945 前端测试用例 + ArchUnit 架构守卫
> **AI 落地**：6 个 AI 决策点（端口抽象 + 多级缓存 + 限流降级）

### 演讲故事线

> **问题**：现代企业级应用必须解决——复杂业务、分布式事务、实时通信、AI 集成，**如何在一个代码库里完整展示？**
> **方案**：选 C2C 资产流转作为业务载体，完整落地 DDD / CQRS / Saga / 事件驱动 / AI 多模态。
> **技术深度**：11 模块 / 16 数据库表 / 9 事件队列 / 6 AI 能力 / 2,214 测试用例（后端 1,269 + 前端 945）。
> **可借鉴**：每个架构决策都有可独立讲解的动机、实现、收益。

---

## 七、未来扩展（架构可承载的方向）

- **虚拟资产深化**：会员权益市场、数字素材库、游戏资产交易（"资产"概念天然支持实物 + 虚拟）
- **品类扩展**：从数码 3C 扩展到潮玩、服饰、图书、运动、云服务订阅
- **AI 资产顾问**：基于用户历史主动推荐"该盘活什么、该领什么"

---

## 八、对外宣传语（陪跑用）

> **EasyOrange** — 选 C2C 资产流转作为业务载体，在真实场景中完整落地 DDD + CQRS + Saga + 事件驱动 + AI 多模态，让大模型在真实业务约束下稳定运行。

---

## 九、核心关键词

`DDD` `CQRS` `Saga` `领域事件` `端口-适配器` `六边形架构` `事件驱动` `RabbitMQ`
`WebSocket` `STOMP` `AI 多模态` `多级缓存` `限流降级` `ArchUnit` `Flyway`
`C2C 直发` `固定价格` `轻资产容器`
